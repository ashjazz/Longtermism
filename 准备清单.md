# 🎯 AI 工程师面试核心准备手册

> **适用岗位：** 生产级 AI 系统工程师（RAG / Agent / Evaluation / Production）
> **核心信号：** 「30% 模型化 + 70% 工程化」—— 要的是能 delivery 的人，不是读论文的人
> **最后更新：** 2026-06-19（对照 2024–2026 最新规范全面修订：原生工具调用、MCP、Agent Skills、Structured Outputs、Contextual Retrieval、Agentic RAG、OTel GenAI 等；模型表已按官方文档重核）

***

## 目录

1. [岗位 JD 逐句拆解](#1-岗位-jd-逐句拆解)
2. [编程语言能力矩阵](#2-编程语言能力矩阵)
3. [RAG 全链路深度解析](#3-rag-全链路深度解析)
4. [Agent 系统设计深度](#4-agent-系统设计深度)
5. [向量数据库决胜点](#5-向量数据库决胜点)
6. [评估体系——面试最大考点](#6-评估体系面试最大考点)
7. [Agent Harness 与框架取舍](#7-agent-harness-与框架取舍)
8. [可观测性与监控](#8-可观测性与监控)
9. [生产环境工程实践](#9-生产环境工程实践)
10. [降级容错与高可用架构](#10-降级容错与高可用架构)
11. [限流与成本控制](#11-限流与成本控制)
12. [语义缓存深度](#12-语义缓存深度)
13. [系统集成与延迟优化](#13-系统集成与延迟优化)
14. [加分项深度准备](#14-加分项深度准备)
15. [面试故事库（STAR 法则）](#15-面试故事库star-法则)
16. [面试常见问题库](#16-面试常见问题库)
17. [反问面试官的好问题](#17-反问面试官的好问题)
18. [代码级自检清单](#18-代码级自检清单)

***

## 1. 岗位 JD 逐句拆解

### 1.1 JD 第一段逐句分析

> "你将构建我们客户真正使用的 AI 系统。这意味着编写提示词（prompts）、设计智能体循环（agent loops）、调优检索管线（retrieval pipelines）、构建评估体系，并在满足客户所需的延迟、成本和可靠性要求下，将这一切投入生产环境。"

| JD 关键词                 | 面试准备方向                                                                               | 需要准备的深度                                      |
| ---------------------- | ------------------------------------------------------------------------------------ | -------------------------------------------- |
| 编写 prompts             | Prompt 工程不是"写一句话"——是模板化、版本化、A/B 测试、回归测试                                              | 能展示 prompt 迭代的完整工程流程                         |
| 设计 agent loops         | 原生 tool calling + reasoning model 驱动的 Agent 循环（ReAct/Plan-Execute/Multi-Agent 为架构模式） | 能讲出原生 tool calling 机制、MCP/Skills、每种模式适用场景和陷阱 |
| 调优 retrieval pipelines | Chunking → Embedding → 检索 → Re-rank → 生成全链路优化                                        | 能说清楚每个环节的选型依据                                |
| 构建评估体系                 | Golden Dataset + 离线评估 + 在线指标 + Human-in-the-loop                                     | **这是面试必问题，准备 10 分钟以上的完整讲述**                  |
| 延迟/成本/可靠性              | 三者 trade-off 分析，模型选型就是在做这个                                                           | 能量化地讨论每个决策的成本和延迟影响                           |

> "这是一项手艺活。我们不需要只读过论文的人。我们正在寻找有实际落地经验（shipped）、并且深知 AI 系统在生产环境中具体是如何失效的人，因为他们曾亲手修复过这些故障。"

**这句话是整个 JD 的灵魂。** 面试官在找：

- 不是"我用过 LangChain"的人
- 而是"LangChain 在生产环境出过什么坑、我怎么修的"的人

**面试准备策略：** 每个技术话题，都要准备一个「出过事 → 修好了 → 学到了」的故事版本。

### 1.2 JD 工作职责逐句映射

| JD 原话                     | 你的准备方向                                  |
| ------------------------- | --------------------------------------- |
| 设计并构建生产级别的 AI 功能          | 准备 1-2 个完整 feature 从 0 到 production 的故事 |
| 构建评估系统以证明你的工作确实有效         | 准备评估体系设计全流程（见第 6 章）                     |
| 提示词版本控制、降级容错、限流、成本控制、可观测性 | 准备每个子项的实战案例（见第 9-12 章）                  |
| 30% 模型化 + 70% 工程化         | 用你的项目经历展示这两个比例                          |
| 当设计方案行不通时，你会尽早指出          | 准备"主动叫停"的案例（见面试故事 4）                    |
| 代码审查、技术文档、午餐分享会           | 证明你不是 solo player                       |

***

## 2. 编程语言能力矩阵

### 2.0 2026 模型格局速查（任何语言都要先认清的底座）

> 模型迭代极快，下表截至 **2026.06**，面试前请用各家官方模型页再核一遍当日模型 ID 与定价。

| 厂商        | 主力模型                                                         | 定位                                 | 上下文                 | 关键能力                                                          |
| --------- | ------------------------------------------------------------ | ---------------------------------- | ------------------- | ------------------------------------------------------------- |
| OpenAI    | GPT-5.5 / GPT-5.4 / GPT-5.4-mini                             | 旗舰 / 性价比 / 低延迟                     | 1M / 1M / 400K      | 原生 tool + 视觉 + **reasoning.effort 调节**（取代了独立的 o-系列）           |
| Anthropic | Claude Fable 5 / Opus 4.8 / Sonnet 4.6 / Haiku 4.5           | 最高能力 / 旗舰 agentic / workhorse / 最快 | 1M / 1M / 1M / 200K | adaptive thinking、extended thinking、原生 tool use、MCP/Skills 首发 |
| Google    | Gemini 3.1 Pro / Gemini 3.5 Flash / Gemini 3 Flash / 2.5 Pro | 复杂推理 / 稳定高性能 / 高吞吐 / 2.5 稳定线       | 1M 级别               | thinking model、长上下文、原生 function calling、Live/API 工具生态         |

**三个 2026 关键信号**：

1. 全三家旗舰已统一进入 **1M 上下文 + 原生工具调用 + 原生推理（reasoning/thinking）** 时代。
2. 「是否思考」不再分叉成两个模型族——用参数（`reasoning.effort` / `effort` / adaptive thinking）运行时调节。
3. 定价呈三档：旗舰 \~$5/$25–30、中端 \~$2.5/$12–15、轻量 \~$0.5–1/$3–5（每百万 token 输入/输出）。

> 官方核验入口：OpenAI Models（`platform.openai.com/docs/models`）、Claude Models Overview（`docs.anthropic.com/en/docs/about-claude/models/overview`）、Gemini API Models（`ai.google.dev/gemini-api/docs/models`）。模型名和价格是最高频过期项，面试前一天必须再核。

> ⚠️ **易错点澄清**：`function_call` ≠ `tools`（前者 2023.11 已被后者取代，见 §4.1）；GPT-4.1 ≠ GPT-5；Claude 3.5/3.7 已退役，当前是 Claude 4.x / Fable 5；Gemini 2.0 Flash 已下线，2.5 是稳定线、3.x 为新旗舰。简历和示例里别写老模型 ID。

### 2.1 结构化输出（Structured Outputs）——所有语言的通用能力

生产环境拿结构化结果，**必须用 strict mode，禁止用正则解析 LLM 自由文本里的 JSON**。三家机制不同：

| 厂商        | 做法                                                                                                                      |
| --------- | ----------------------------------------------------------------------------------------------------------------------- |
| OpenAI    | `response_format: {type:"json_schema", strict:true, schema}` 或 `responses.parse(text_format=Pydantic模型)`，100% schema 遵守 |
| Anthropic | 无独立 strict response\_format，通过 **tool use 强制结构**（把输出 schema 定义成 tool 并 `tool_choice` 强制调用），或 JSON mode + 后校验            |
| Google    | `response_schema` + `response_mime_type: application/json`                                                              |

**strict mode 三条硬约束**：① `additionalProperties: false` ② 所有字段列入 `required`（可选用 `type:["string","null"]`）③ 根对象是 object。

### 2.2 Python（最高优先级）

面试中需要展示的 Python 能力层次：

**Level 1：API 调用（不够）**

```python
# 这种程度只能证明你"用过"，不是"能交付"
from openai import OpenAI
client = OpenAI()
response = client.chat.completions.create(
    model="gpt-5.5",
    messages=[{"role": "user", "content": "Hello"}]
)
```

**Level 2：生产级服务（面试需要展示的）**

```python
import asyncio
import logging
import time
from typing import Any, AsyncIterator

import tiktoken
from openai import APIError, APITimeoutError, AsyncOpenAI, RateLimitError
from tenacity import retry, stop_after_attempt, wait_exponential
from pydantic import BaseModel, Field

logger = logging.getLogger(__name__)

class ChatMessage(BaseModel):
    role: str
    content: str

class ChatRequest(BaseModel):
    messages: list[ChatMessage]
    model: str = "gpt-5.4-mini"
    max_tokens: int = Field(default=1024, le=4096)
    temperature: float = Field(default=0.0, ge=0.0, le=2.0)

class ChatResponse(BaseModel):
    content: str
    usage: dict[str, Any]  # prompt_tokens, completion_tokens, reasoning_tokens...
    model: str
    latency_ms: float

class LLMService:
    """生产级 LLM 服务封装"""
    
    def __init__(self, primary_model: str = "gpt-5.4",
                 fallback_model: str = "gpt-5.4-mini"):
        self.client = AsyncOpenAI()
        self.primary = primary_model
        self.fallback = fallback_model
        self._tokenizer = tiktoken.encoding_for_model(primary_model)
    
    def _count_tokens(self, text: str) -> int:
        return len(self._tokenizer.encode(text))
    
    @retry(
        stop=stop_after_attempt(3),
        wait=wait_exponential(multiplier=1, min=1, max=30),
        retry=lambda e: isinstance(e, (RateLimitError, APITimeoutError))
    )
    async def chat(self, request: ChatRequest) -> ChatResponse:
        """带重试、降级、token 预算的聊天接口"""
        # Token 预算检查
        estimated_tokens = sum(
            self._count_tokens(m.content) for m in request.messages
        )
        safe_request = request
        if estimated_tokens > 100_000:
            # 不原地修改 request，返回一个截断后的新副本
            safe_request = request.model_copy(
                update={"messages": self._truncate_context(request.messages)}
            )
        
        start = time.monotonic()
        try:
            response = await self._call_api(safe_request, safe_request.model)
        except (RateLimitError, APIError) as e:
            if safe_request.model == self.primary:
                logger.warning("Primary model failed, falling back", exc_info=True)
                response = await self._call_api(safe_request, self.fallback)
            else:
                raise
        
        latency = (time.monotonic() - start) * 1000
        return ChatResponse(
            content=response.choices[0].message.content,
            usage=dict(response.usage),
            model=response.model,
            latency_ms=latency
        )
    
    async def _call_api(self, request: ChatRequest, model: str, stream: bool = False):
        return await self.client.chat.completions.create(
            model=model,
            messages=[m.model_dump() for m in request.messages],
            max_tokens=request.max_tokens,
            temperature=request.temperature,
            stream=stream,
        )

    def _truncate_context(self, messages: list[ChatMessage]) -> list[ChatMessage]:
        # 生产实现通常会做摘要压缩；这里示意保留最近窗口
        return messages[-20:]

    async def stream(self, request: ChatRequest) -> AsyncIterator[str]:
        """流式响应 - 生产环境必备"""
        stream = await self._call_api(request, request.model, stream=True)
        async for chunk in stream:
            if chunk.choices[0].delta.content:
                yield chunk.choices[0].delta.content
```

**Level 3：你能在面试中展示的进阶能力**

- `asyncio` 异步编程：协程、事件循环、并发控制（`asyncio.Semaphore`）
- `Pydantic` 数据校验和配置管理（`BaseSettings`）
- `pytest` 测试：mock LLM 调用、fixture 管理测试数据
- `FastAPI` / `Starlette`：构建高性能 AI API 服务
- 类型注解：`mypy` 严格模式、`Protocol` 和 `Generic` 的使用

### 2.3 TypeScript / Node（次选）

如果是 TS 背景，需要展示：

- 流式响应的 `ReadableStream` / `AsyncIterator` 处理
- Edge 部署（Vercel AI SDK、Cloudflare Workers）
- Zod 做 schema validation
- 你如何用 TS 构建过 AI 服务的完整案例

### 2.4 Go（加分，本项目语言）

如果是 Go 背景：

- 高并发 goroutine + `errgroup`/`sync` 管理 LLM 调用池与并发扇出
- `context.Context` 贯穿超时/取消（AI 调用默认 60s 超时靠它实现）
- SSE 流式：用 `net/http` 的 `Flusher` 或 GoFrame `ghttp` 流式响应逐 token 下发
- 原生 tool calling：消费 provider 返回的 `tool_calls` 结构体，执行后回传 `tool_result`（见 §4）
- channel + select 处理流式 chunk、并发检索结果聚合
- 你如何用 Go 构建 AI 服务的完整案例（gRPC 微服务 / HTTP API）

> 本框架用 Go + GoFrame v2 实现，`pkg/ai/llm/` 用 interface 抽象多 provider，`ChatStream` 返回 `<-chan ChatChunk`——这正是 Go 在 AI 工程里的典型发力点。

***

## 3. RAG 全链路深度解析

> **面试核心问题：** "从技术选型到上线，讲一讲你做过的最复杂的 RAG 系统"
> **你需要在 5-10 分钟内讲出：** 业务问题 → 文档处理 → Chunking → Embedding → 向量库 → 检索策略 → 生成 → 评估 → 生产事故 → 优化

### 3.1 Chunking 策略（面试区分点）

这是面试中区分"真干过"和"看过文章"的核心话题。不同的 chunking 策略会显著影响召回率，但具体提升高度依赖语料、embedding、top-k 和评估集；面试中不要背单一数字，应该展示你如何用自有 golden set 做 A/B benchmark。

#### 策略一：Recursive Character Splitting（默认首选）

```
适用场景：通用文档（博客、wiki、技术文档）
推荐参数：chunk_size=400-512 tokens, overlap=10-20%
原理：按分隔符层级递归拆分（段落→句子→词→字符）
优点：实现简单、计算成本低、尊重自然语言边界
缺点：不理解语义、表格和代码可能被切断
```

**分层分隔符设计：**

```python
# 通用文档
separators = ["\n\n", "\n", ". ", " ", ""]

# 代码文档
separators = ["\n\nclass ", "\n\ndef ", "\n\n", "\n", " ", ""]

# Markdown 文档
separators = ["\n## ", "\n### ", "\n\n", "\n", ". ", " ", ""]
```

**Overlap 的作用：** 确保跨 chunk 边界的上下文不丢失。比如一个概念横跨两个 chunk，overlap 让相邻 chunk 共享部分内容，提高检索召回率。

#### 策略二：Semantic Chunking

```
原理：对每个句子生成 embedding，计算相邻句子的余弦相似度
      在相似度骤降处（语义边界）切分
优点：保留语义完整性，召回率可提升 3-9%
缺点：需要对每个句子做 embedding，成本高
适用：对精度要求高、文档结构不规整的场景
```

> ⚠️ **2025 定位调整**：Semantic Chunking **已不再是推荐默认**——效果不稳定、成本高（要为每个句子跑 embedding 判断边界）。当前共识是 **recursive + overlap 作为稳健默认**，再用「Contextual Retrieval」弥补上下文丢失，性价比更高。Semantic Chunking 降级为"可选方案之一"。

**实现思路：**

```python
def semantic_split(sentences, embedding_model, threshold=0.7):
    embeddings = [embedding_model.encode(s) for s in sentences]
    chunks = []
    current_chunk = [sentences[0]]
    
    for i in range(1, len(sentences)):
        similarity = cosine_similarity(embeddings[i-1], embeddings[i])
        if similarity < threshold:  # 语义断裂点
            chunks.append(" ".join(current_chunk))
            current_chunk = [sentences[i]]
        else:
            current_chunk.append(sentences[i])
    return chunks
```

#### 策略三：Small-to-Big / 父子文档检索

```
核心思想：
  索引时：用小 chunk（精确匹配，如 200 tokens）
  检索时：返回大 chunk 或父文档（完整上下文，如 800-2000 tokens）

这样做的好处：
  - 检索阶段用小 chunk 提高精度
  - 生成阶段用大 chunk 保证上下文完整
  - 解决了"chunk 太小丢上下文，太大丢精度"的经典矛盾
```

**实现方式：**

- **Parent Document Retriever**（LangChain）：索引小 chunk，返回父文档
- **Sentence Window Retrieval**（LlamaIndex）：索引句子，返回前后 N 句的窗口
- **手动实现：** 在 metadata 中存储 `parent_doc_id`，检索后通过 ID 取回完整内容

#### 策略四：Late Chunking / ColBERT 风格

```
原理：不在索引前分 chunk，而是先对整个文档做 embedding
      然后在检索时对文档的 token 级 embedding 做匹配
优点：保留了全局上下文，不存在"在错误位置切分"的问题
缺点：索引和检索成本高，需要专门的模型（如 ColBERT）
适用：长文档、法律文书、合同等对精确匹配要求极高的场景
```

#### 策略五：Agentic Chunking

```
原理：用 LLM 分析文档结构，智能决定切分点
优点：理论上最优，能理解表格、列表、多级标题的语义结构
缺点：成本最高、速度最慢、LLM 可能不稳定
适用：少量高价值文档的处理
```

#### 策略六：Contextual Retrieval（Anthropic 2024.09，强烈推荐的新默认增强）

```
问题：传统 chunking 把文档切成独立小块，每个 chunk 丢失了"它在整篇文档里的位置/主题"
       导致 embedding 与 BM25 都容易误检索
做法：对每个 chunk，先用 LLM 生成 50–100 token 的"定位上下文前缀"，拼到原文前面再做 embedding/BM25
       检索时用带上下文的版本
核心：它不是替代上述切分策略，而是叠加在任意切分之上的"上下文增强"层
```

**关键数字（面试高频，来源：Anthropic《Introducing Contextual Retrieval》，2024.09）**：

| 叠加技术                                          | top-20 chunk 检索失败率下降 |
| --------------------------------------------- | -------------------- |
| Contextual Embeddings 单独                      | **35%**              |
| Contextual Embeddings + Contextual BM25（混合检索） | **49%**（5.7% → 2.9%） |
| 上述 + Reranking（Cohere Rerank v3）              | **67%**              |

**成本控制（生产关键）**：朴素做法对每个 chunk 跑 LLM 很贵，但每次请求都带同一份完整文档——Anthropic 原文给出的做法是用 **prompt caching** 缓存整篇文档，文档 token 成本可降最高 \~90%、延迟降 >2x；在其 800-token chunk / 8k-token document / 100-token context 的假设下，约 **$1.02 / 百万 document tokens**。

```python
# 离线一次性：对每个 chunk 预生成上下文前缀
context_prompt = f"""<document>{WHOLE_DOCUMENT}</document>
Here is the chunk we want to situate within the whole document:
<chunk>{CHUNK_CONTENT}</chunk>
Please give a short succinct context to situate this chunk within
the overall document for retrieval. Output only the context."""
context = llm(context_prompt)          # 用 prompt caching 复用 WHOLE_DOCUMENT
indexed_chunk = f"{context}\n{CHUNK}"  # 带上下文再 embedding / BM25
```

#### 策略七：Late Chunking（Jina AI 2024.09）

```
原理：与"先切分再 embedding"相反——先把整篇文档喂给支持 8K token 的长上下文 embedding 模型，
       模型内部 attention 让每个 token 的表示已编码全文上下文；输出 token 级向量后，
       按 chunk 边界对 token 向量做 mean-pooling 得到 chunk embedding
优点：每个 chunk embedding 天然带全文上下文，不存在"在错误位置切分"问题；无额外 LLM 调用
缺点：必须用长上下文 embedding 模型（如 jina-embeddings-v2/v3），文档须能塞进其上下文窗口
适用：能塞进 embedding 模型上下文窗口的文档
```

**与 Contextual Retrieval 的区别（面试要能对照）**：

| <br /> | Contextual Retrieval（Anthropic）   | Late Chunking（Jina）         |
| ------ | --------------------------------- | --------------------------- |
| 成本     | 每个 chunk 跑 LLM 生成前缀（靠 caching 压低） | 一次 embedding 调用，无额外 LLM 调用  |
| 依赖     | 任意 embedding 模型                   | **必须长上下文 embedding 模型**（8K） |
| 增强方式   | 后处理加文本前缀                          | 前处理改池化顺序                    |

> 两者解决同一个问题（chunk 丢失全局上下文）但机制相反：前者加文本、后者改池化顺序。可二选一，按是否已有长上下文 embedding 模型决定。

#### Chunking 策略选择决策树（2026 更新）

```
你的文档类型是什么？
├── 规整的文本（博客、wiki）
│   └── Recursive Character Splitter (chunk_size=400-512, overlap=50-100)
├── 非规整文本（扫描件 OCR、混合格式）
│   └── Semantic Chunking + Small-to-Big 检索
├── 代码文件
│   └── Recursive + 代码感知分隔符
├── 表格密集型文档
│   └── Page-level + 表格解析预处理
├── 法律/合同（长文档精确匹配）
│   └── Late Chunking / ColBERT
└── 混合作战
    └── 按文档类型路由到不同策略
```

> **2026 推荐配方（通用增强层）**：无论选哪种切分，生产 RAG 的稳健默认组合是
> **recursive 切分打底 + small-to-big 检索（匹配用小 chunk、生成用大 chunk）+ Contextual Retrieval 上下文增强**，
> 结构化文档（PDF/表格）再用 LlamaParse / Unstructured 做标记/布局感知解析。
> Late Chunking 在你已采用长上下文 embedding 模型时作为 Contextual Retrieval 的平替。

> ⚠️ **PDF/表格 chunking 仍是 RAG 最大痛点**（社区 2026 共识："PDF parsing for RAG is still a mess"，表格碎片是得分最低的 chunk）。主流工具：**LlamaParse**（表格/图表结构化抽取强）、**Unstructured Advanced**（layout-aware element-type 切分）。实践建议：解析后做清理（去掉表格碎片、孤儿标题）再 embedding。

### 3.2 Embedding 模型选型

| 模型                       | 厂商        | 默认维度       | 可截断维度             | 最大 Token | 特点                                             | 适用场景                          |
| ------------------------ | --------- | ---------- | ----------------- | -------- | ---------------------------------------------- | ----------------------------- |
| `text-embedding-3-large` | OpenAI    | 3072       | 任意（MRL）           | 8191     | 通用闭源标杆                                         | 对检索质量要求高的通用场景                 |
| `text-embedding-3-small` | OpenAI    | 1536       | 任意（MRL）           | 8191     | 便宜、快                                           | 对成本敏感的通用场景                    |
| `voyage-3-large`         | Voyage AI | 1024       | 256/512/1024/2048 | 32000    | 2025.01 起 MTEB 多域第一                            | 对检索质量要求极高、长文档                 |
| `voyage-3 / -lite`       | Voyage AI | 1024 / 512 | MRL               | 32000    | 比 OpenAI v3 成本更低、维度更小                          | 性价比通用                         |
| `embed-v4`               | Cohere    | 1536       | 256/512/1024/1536 | 128000   | **多模态**（文本+图像统一空间），2025.04                     | 多语言、多模态                       |
| `jina-embeddings-v3`     | Jina      | 1024       | 32–1024           | 8192     | **Task-LoRA 指令适配**（retrieval/cluster/classify） | 需要任务适配、**Late Chunking 原生支持** |
| `bge-m3`                 | BAAI（开源）  | 1024       | —                 | 8192     | **三合一**：dense + sparse + ColBERT 多向量           | 自部署、要混合检索                     |
| `gemini-embedding-001`   | Google    | 3072       | 768/1536/3072     | 8191     | task type 参数；多语                                | Google 生态                     |

**三个 2024 后普遍化的关键术语（面试要会解释）**：

- **Matryoshka Representation Learning（MRL）/ 可降维**：训练时让向量前缀也保持语义，可截断到更低维度省存储/算力。OpenAI v3、Voyage 3、Jina v3、Cohere v4、Gemini embedding 全部支持。
- **指令调优 / Task-LoRA**：embedding 时带任务指令（如 `query:` / `passage:`），让同一模型适配检索/聚类/分类。
- **非对称检索**：query 侧和 document 侧用不同前缀/指令（如 Cohere 的 `search_document` vs `search_query`），2024 后成标配。

**关键面试点：**

- 维度选择：高维=更精确但更贵更慢；用 MRL 降维是存储/算力/精度的调节旋钮。
- 为什么不能直接用通用 embedding 做专业领域？→ embedding 的分布和训练语料相关，私有域可能掉点。
- 怎么验证 embedding 质量？→ **MTEB 排行榜 + 你自己的检索 benchmark**，缺一不可。

**MTEB 怎么用（操作步骤）**：MTEB（Massive Text Embedding Benchmark）= 8 类任务 × 58+ 数据集 × 112+ 语言。Leaderboard：<https://huggingface.co/spaces/mteb/leaderboard>

1. **按域过滤**（Legal/Medical/Code/Finance…），别只看总均值；
2. **看 retrieval 子任务分**（RAG 关心检索），别看聚类分；
3. **本地复跑**：`mteb` 包 + `mteb.get_tasks(task_types=["Retrieval"])`；
4. **务必在自有 golden set 上回归**——通用模型在私有域可能掉点（呼应 §6）。

### 3.3 检索策略进阶

#### 基础检索 → 混合检索 → 高级检索

```
Level 1: 纯向量检索
  query → embedding → vector DB top_k → context → LLM
  问题：关键词匹配差（"Apple 公司"和"苹果公司"可能不相似）

Level 2: 混合检索（Hybrid Search）
  向量检索 top_k + BM25/tf-idf 关键词检索 top_k
  → 融合排序（RRF: Reciprocal Rank Fusion）
  → context → LLM
  问题：检索到的内容太多，LLM 上下文窗口压力大

Level 3: 混合检索 + Re-ranking
  混合检索 top_100 → Cross-encoder Re-ranker → top_k
  → context → LLM
  这是目前生产环境的主流方案

Level 4: Query 改写 + 多路召回 + Re-rank
  user_query → Query Rewriting (LLM) → 多个改写 query
  → 多路并行检索 → 去重 + RRF 融合 → Re-rank
  → context → LLM
```

**关键组件详解：**

**Query Rewriting（查询改写）：**

```python
# 将用户口语化问题改写为更利于检索的查询
# 例："上次那个文件怎么用来着？" → "文件上传功能使用说明"
rewrite_prompt = """
Rewrite the user's query into a self-contained search query 
for a document retrieval system. Remove ambiguity and add 
necessary context.
User query: {user_query}
Search query:
"""
```

**HyDE (Hypothetical Document Embeddings)：**

```
流程：
1. 用 LLM 根据 query 生成一个假设的理想答案
2. 对这个假设答案做 embedding
3. 用这个 embedding 去检索真实文档
4. 将检索到的真实文档 + 原始 query 发给 LLM 生成最终答案

原理：假设答案的 embedding 比问题本身更接近真实文档的 embedding 分布
```

**RRF (Reciprocal Rank Fusion) 融合公式：**

```
RRF_score(d) = Σ(1 / (k + rank_i(d)))

其中 k 是常数（通常 60），rank_i(d) 是文档 d 在第 i 个排序列表中的排名

这个公式的优雅之处：不需要归一化不同分数，只需要排名
```

> **RRF 的两个用途**（2026 补充）：① 混合检索融合（dense 向量 + sparse BM25）；② **Multi-Query 融合**（下方 RAG-Fusion）。

#### 进阶范式一：RAG-Fusion / Multi-Query（query 侧增强）

```
1. Multi-Query Generation：LLM 把用户原 query 改写成 3–5 个不同表述/角度的变体
2. 并行检索：每个变体独立向量检索 top_k
3. RRF 融合：score(d) = Σ 1/(k + rank_i(d))，在多个变体中都靠前的文档得分高
4. 融合后 top_k 喂给 LLM 生成
```

**核心价值**：单一 query 表述无法覆盖所有相关文档，多 query + RRF 提升召回与排序鲁棒性。这是"query 侧增强"，与"文档侧增强（Contextual Retrieval / Late Chunking）"正交，可叠加。

#### 进阶范式二：Agentic RAG（RAG 作为 Agent 决策工具，2024–2025）

把 RAG 从"线性流水线"变成 agent 的**决策工具**（交叉引用 §4）。Agent 自主决定：

- **是否检索**（对已知简单问题直接回答，省一次检索）
- **检索几次**（复杂问题多轮迭代检索）
- **Query 改写**：HyDE、Sub-query 分解、Step-back prompting、Multi-query
- **自反思/纠错**：Self-RAG（对检索文档打分 grade、对结果自评）、CRAG（检索质量差则触发 query 重写或 web 搜索兜底）

| 维度    | 经典线性 RAG  | Agentic RAG             |
| ----- | --------- | ----------------------- |
| 检索触发  | 总是检索      | 按需检索（agent 判断）          |
| 检索次数  | 固定 1 次    | 动态，可多轮                  |
| Query | 用户原 query | agent 自动改写/分解           |
| 错误恢复  | 无         | Self-RAG/CRAG 自纠、回退 web |
| 延迟/成本 | 低、可预测     | 高、不可预测                  |

> ⚠️ **生产陷阱**：Agentic RAG 容易陷入"检索-重写-再检索"的无限循环和 token 爆炸。必须配 **步数上限 + 循环检测 + token 预算**（§4.8），并在 §6 评估里证明它带来的准确率提升值得额外延迟/成本。Self-RAG / CRAG 不应作为独立小节堆砌，而是 Agentic RAG 的两个特例/退化形式。

#### 进阶范式三：GraphRAG（Microsoft 2024，补全"全局总结类"问题）

```
机制：
1. 用 LLM 从文档抽取实体与关系，构建知识图谱
2. 用 Leiden 算法做层次化社区检测（hierarchical communities）
3. LLM 为每个社区、每个层级生成摘要
4. 检索：
   - Local Search（具体问题）：从实体 embedding 出发，向相邻实体/chunk/社区遍历
   - Global Search（跨文档、总结性问题）：聚合多个社区摘要，Map-Reduce 生成答案
```

**适用与不适用**：

- ✅ 适合：**跨文档、全局性、总结性问题**（如"这份财报里所有关于供应链风险的主题有哪些"）——这正是经典向量 RAG 的弱项
- ❌ 不适合：简单事实点查询（成本/延迟不如向量 RAG）、频繁更新的数据（图重建昂贵）

> ⚠️ **成本陷阱**：建图阶段对每篇文档都跑 LLM 抽实体，**索引成本远高于向量 RAG**（通常 10x+ token）。必须用 prompt caching、增量更新。GraphRAG 是"离线重索引"型开销，与向量 RAG 互补而非替代。

### 3.4 RAG 生产环境常见故障模式

| 故障               | 原因                          | 解决方案                                             |
| ---------------- | --------------------------- | ------------------------------------------------ |
| 检索到不相关内容         | Chunking 不当、embedding 模型不匹配 | 调整 chunk 大小、切换 embedding、加 re-rank               |
| 检索到的内容不够         | 文档没有被正确切分或索引                | 检查 ingestion pipeline、增加 overlap、采用 small-to-big |
| 上下文太长超出 token 限制 | 检索了太多文档                     | 严格控制 top\_k、增加 re-ranking 精排                     |
| 回答引用了错误来源        | 检索正确但 LLM 搞混了来源             | 要求 LLM 逐句标注引用、用 structured output                |
| 相同问题不同答案         | LLM 非确定性 + 检索结果不稳定          | temperature=0、缓存策略、固定检索 pipeline                 |
| 延迟太高             | 全链路串行执行                     | 并行化检索、用更小模型、语义缓存                                 |

### 3.5 RAG 架构决策清单（面试中可以主动展示的框架）

```
1. 文档处理
   □ PDF 解析选择了哪个库？（PyMuPDF / Unstructured / LlamaParse）
   □ 表格怎么处理？（保留 markdown 表格 / 单独存储结构化数据）
   □ 图片/图表怎么处理？（跳过 / 多模态 embedding / 单独 OCR）

2. Chunking
   □ 用的什么策略？chunk_size 和 overlap 是多少？
   □ 为什么选这个参数？（通过实验数据支撑）
   □ 是否使用了父子文档 / small-to-big / 句子窗口（检索用小 chunk、生成用大 chunk）？
   □ 是否叠加了 Contextual Retrieval（上下文前缀增强）？或 Late Chunking？

3. Embedding
   □ 选的哪个模型？为什么？（对照 MTEB 检索子任务 + 自有 benchmark）
   □ 维度是多少？是否用了 Matryoshka（MRL）降维？
   □ 是否用了指令调优 / 非对称检索（query/passage 不同前缀）？
   □ 是否有领域适配需求？

4. 向量数据库
   □ 选型理由？（见第 5 章）
   □ 索引用 HNSW 还是 IVF？是否用了量化（scalar/binary/PQ）？
   □ 参数怎么调的？metadata filtering 怎么设计的？

5. 检索
   □ 纯向量检索还是混合检索？是否用了原生 hybrid（BM25+向量）？
   □ 用了 re-ranking 吗？用的什么模型？
   □ 是否做了 query rewriting / HyDE / Multi-Query（RAG-Fusion）？
   □ 是否需要 GraphRAG 回答"全局总结类"问题？

6. 生成
   □ 系统 prompt 怎么设计的？（展示版本迭代）
   □ 是否用 Structured Outputs / strict mode 强制结构化（含引用编号）？
   □ 怎么控制幻觉？（引用标注、temperature=0、不确定性表达）
   □ 怎么处理"我不知道"的情况？

7. 评估
   □ 用什么指标衡量检索质量？（Recall@k, MRR, NDCG）
   □ 用什么指标衡量生成质量？（见第 6 章）
   □ 评估集怎么建的？多久更新一次？

8. 长上下文 vs RAG（2026 必答）
   □ 你的语料规模、QPS、是否需要源可追溯？（见 §16 问题 5 的 nuanced 结论）
   □ 是否采用"RAG 粗筛 + 长上下文精读"混合范式？
```

***

## 4. Agent 系统设计深度

> **面试核心问题：** "设计一个 Agent 来处理用户的复杂任务，你会怎么考虑？从架构、安全、可靠性三个维度回答。"
>
> ⚠️ **2024–2026 最重要的范式转移**：Agent 的实现已经从"提示词输出 `Thought:/Action:/Observation:` 再正则解析"
> 迁移到**原生工具调用（native tool/function calling）+ 推理模型（reasoning model）**。
> 旧的文本解析式 ReAct 在生产中已被淘汰（脆弱、费 token、解析易错）。本章已据此重写。
> 若面试官问"你会怎么写 Agent"，**先讲原生 tool calling，把 ReAct 当历史背景**。

### 4.0 先建立 2026 心智模型：三层能力栈

```
┌──────────────────────────────────────────────────────────┐
│  Agent Skills（Anthropic 2025.10）                         │  ← 过程指导层：何时/如何做某领域的事
│  打包"指令 + 脚本 + 资源"，渐进式披露（SKILL.md）           │
├──────────────────────────────────────────────────────────┤
│  MCP（Model Context Protocol，Anthropic 2024.11 开源）      │  ← 连接/传输层：去哪找、怎么连这些能力
│  工具的标准化交付协议，把 N×M 集成降为 N+M                   │
├──────────────────────────────────────────────────────────┤
│  Tools / Function Calling（原生，各家 API 内置）            │  ← 执行层：具体执行什么
│  模型输出结构化 tool_call，而非自然语言文本                  │
└──────────────────────────────────────────────────────────┘
```

记住这个三层关系：**Skills 编排 MCP/Tools，MCP 标准化交付 Tools，Tools 是真正被执行的函数。** 三者互补不替代。

### 4.1 原生工具调用（Tool / Function Calling）——现代 Agent 的地基

#### 4.1.1 演进时间线（OpenAI 主导，逐字核实自官方文档）

| 时间               | 事件                                                                                                  | 关键字段                                                 |
| ---------------- | --------------------------------------------------------------------------------------------------- | ---------------------------------------------------- |
| 2023.06          | OpenAI 推出 Function Calling                                                                          | `functions` + `function_call`                        |
| 2023.11 (DevDay) | **`function_call`** **被** **`tools`** **+** **`tool_choice`** **取代**；旧参数标记弃用；并行工具调用随 GPT-4 Turbo 上线 | `tools`、`tool_calls`                                 |
| 2024.08.06       | **Structured Outputs** 发布，宣称 100% schema 遵守                                                         | `response_format: {type:"json_schema", strict:true}` |
| 2024+            | strict mode 扩展到 function calling；并行 tool call 默认开启；流式 tool call                                     | `strict:true`、`parallel_tool_calls`                  |
| 2025.03.11       | **Responses API** 发布，取代 Assistants API，内置 web\_search/file\_search/computer\_use、原生支持 MCP           | `function_call_output`、`call_id`                     |

> **一句话答面试官**：「`function_call` 早已不是最新 API——它在 2023.11 就被 `tools` 统一取代了。现在声明工具用 `tools` 数组（JSON Schema），模型返回结构化的 `tool_call`，我们执行后用 `tool_call_output` 回传结果。」

#### 4.1.2 现代工具声明（tools schema，OpenAI Responses API）

```python
tools = [{
    "type": "function",          # 固定 "function"
    "name": "get_weather",
    "description": "查询某城市当前天气。",
    "strict": True,              # 推荐：强制 100% 遵守 schema
    "parameters": {
        "type": "object",
        "properties": {
            "location": {"type": "string",
                         "description": "城市，如 '上海'"},
            "units": {"type": ["string", "null"],      # strict 模式下可选字段的写法
                      "enum": ["celsius", "fahrenheit", None]}
        },
        "required": ["location", "units"],   # strict 模式：所有字段必须列入 required
        "additionalProperties": False          # strict 模式强制
    }
}]
```

模型返回（在 `response.output` 里）零到多个 `function_call`：

```json
{"type": "function_call", "call_id": "call_abc",
 "name": "get_weather", "arguments": "{\"location\":\"上海\"}"}
```

你执行后**回传结果**（注意 Responses API 用 `call_id`）：

```python
input.append({"type": "function_call_output",
              "call_id": item.call_id,
              "output": json.dumps(result)})
```

然后带着这段历史再次调用模型，它据此继续推理或给出最终答案——这就是现代 Agent 循环的内核。

#### 4.1.3 Structured Outputs / strict mode（强制结构化输出）

strict mode 的三条硬约束（面试必背）：

1. 每个 object 必须 `additionalProperties: false`
2. 所有字段必须列入 `required`（可选字段用 `type: ["string","null"]` 表达）
3. 根对象必须是 object

**收益**：① 可靠类型安全（无需正则解析 + 重试）② refusal 可编程检测 ③ 无需强语气提示词。官方明确「永远优先 Structured Outputs 而非旧的 JSON mode」。

**`tool_choice`** **四种模式**：`"auto"`（默认）/ `"required"`（至少调一个）/ `{"type":"function","name":"X"}`（强制调特定函数）/ `"none"`。

#### 4.1.4 OpenAI vs Anthropic Claude 机制对照（高频考点）

| 维度     | OpenAI                                             | Anthropic Claude                                                             |
| ------ | -------------------------------------------------- | ---------------------------------------------------------------------------- |
| 工具调用载体 | 独立的 `function_call` item                           | message 内的 `tool_use` **content block**                                      |
| 停止信号   | finish\_reason / 输出 item 类型                        | `stop_reason: "tool_use"`                                                    |
| 结果回传   | `function_call_output`（带 `call_id`）                | **`user`** **消息里的** **`tool_result`** **block**（带 `tool_use_id`）             |
| 结构化输出  | `response_format: json_schema + strict`（原生 strict） | 通过 **tool use 强制结构**（把输出 schema 定义成一个 tool 并 `tool_choice` 强制调用），或 JSON mode |
| 工具类型   | client tools                                       | client tools + **server tools**（如 `web_search`，Anthropic 侧执行）                |

> **关键差异**：OpenAI 有原生 strict JSON schema；Claude 没有独立 strict response\_format，靠"把输出当 tool 调用"来强制结构。面试讲"你怎么保证输出是合法 JSON"时，要能说出两家不同做法。

#### 4.1.5 并行 / 流式工具调用

- **并行**：模型单轮可返回多个 `function_call`，可并行执行；用 `parallel_tool_calls: false` 关闭。
- **流式**：先收到 `output_item.added`（含 name/id），再连续收到 `function_call_arguments.delta`（参数逐 chunk 增量），最后 `.done`。需按 `output_index` 聚合。

### 4.2 ReAct：从主推范式降级为历史背景

旧手册把「`Thought:/Action:/Observation:` 文本解析」当作 Agent 实现核心——**这在 2026 是反模式**。但 ReAct 的思想史地位仍要会讲：

```
ReAct（Yao et al., 2022）确立了「推理-行动交替」的循环范式：
  Thought → Action → Observation → ... → Final Answer

它的两项职责，如今各自由更可靠的机制承接：
  ① Action（行动）  → 原生 tool calling（结构化 schema，不再解析自然语言）
  ② Thought（思考） → 推理模型（reasoning model）把推理放进专属 reasoning/thinking item，
                       对开发者部分隐藏，不再占用用户可见输出

结论：有了原生 tool calling 做 acting、推理模型做 reasoning，
       就不再需要把推理和行动用自然语言拼在一起再正则切分。
```

**生产里为什么淘汰文本解析式 ReAct**：① LLM 输出不守格式，解析脆弱 ② Thought 输出浪费大量 token ③ 无法利用 strict mode 的结构保证 ④ 推理模型时代，显式 Thought 已无必要。

> ⚠️ 注意：旧式"鲁棒 action 解析器"（一堆正则兜底）的代码不要写进简历或面试白板——会被当作没跟上时代。**现代写法是直接消费 API 返回的结构化** **`tool_call`。**

### 4.3 MCP（Model Context Protocol）——工具的标准化交付层

**是什么**：Anthropic **2024.11** 开源的开放标准（现捐赠给 Linux Foundation 下 Agentic AI Foundation，厂商中立）。

**解决什么**：N×M 集成地狱——每个 LLM × 每个工具都要单独写适配。MCP 把它降为 **N+M**：每个工具实现一次 MCP server，每个 host 实现一次 MCP client。

**架构**：

| 组件         | 角色                                      |
| ---------- | --------------------------------------- |
| **Host**   | LLM 应用（如 Claude Desktop、IDE），用户入口       |
| **Client** | host 内部组件，每个 client 连一个 MCP server（1:1） |
| **Server** | 轻量程序，通过协议暴露数据/工具/能力                     |

**四类 primitives**：

| Primitive     | 方向            | 说明                               |
| ------------- | ------------- | -------------------------------- |
| **Resources** | server→client | 只读数据源（文件、DB 记录）                  |
| **Tools**     | server→client | 可执行函数（即 function calling 的标准化交付） |
| **Prompts**   | server→client | 预定义模板                            |
| **Sampling**  | client→server | server 反向请求 host 的 LLM           |

**Transport 演进（2025 重要变更）**：`stdio`（本地标准）/ `Streamable HTTP`（**2025-03-26 新标准**，取代已弃用的 HTTP+SSE，单端点）/ `SSE`（已弃用）。

**生态现状（2025–2026）**：2025.03 OpenAI Agents SDK 内置 MCP、Responses API 增加 remote MCP 支持；各大厂商陆续接入，MCP 成为事实标准。

**MCP 与 function calling 的关系（必会）**：MCP 是「工具的标准化交付/传输层」，**底层仍是 tool calling**。MCP server 把声明的 tools 暴露给 client，client 转成宿主 LLM 的 `tools` 数组——模型调用时走的还是原生 function calling。MCP 解决"工具怎么被发现、连接、复用"，不取代调用机制。

### 4.4 Agent Skills（Anthropic 2025）——过程指导层

**是什么**：比 tool 更高层的**打包能力**——一个 skill = 一个文件夹，含 `SKILL.md`（指令 + metadata）+ 可选脚本/模板/资源。

**核心设计：渐进式披露（progressive disclosure）**。模型先只读 `SKILL.md` 头部轻量 metadata（够它判断**何时**该用），需要时再用 bash 读更多 bundled 文件。经验法则：**超过 200 行的 skill 应拆分**。

**三层关系**（最清晰心智模型）：

| 层        | 组件               | 回答的问题        |
| -------- | ---------------- | ------------ |
| 过程指导（最高） | **Agent Skills** | 怎么把这个领域的事做好？ |
| 连接/传输（中） | **MCP**          | 去哪找/连这些能力？   |
| 执行（最低）   | **Tools**        | 具体执行什么？      |

> Skills **不取代** MCP/Tools，而是**编排**它们——Skills 决定**何时、如何**用下层工具。

### 4.5 Agent 架构模式谱系（现代版）

参考 Anthropic 2024.12.19《Building Effective Agents》（基于观察几十个客户团队），先区分两个概念：

- **Workflow（工作流）**：预编排的代码路径，LLM 担任步骤执行者。
- **Agent（智能体）**：LLM **自主决定**自己的流程与工具使用。

**六种执行拓扑**（权威，可直接展示）：

```
1. Prompt Chaining（提示链）：prompt A → gate → prompt B → ...，线性
   适用：任务可拆成固定子任务（如 先翻译 再校对 再润色）

2. Routing（路由分发）：分类器把请求路由到不同处理分支
   适用：输入类型明确可分（客服分流、简单/复杂查询分流）

3. Parallelization（并行）：
   - Sectioning：同一任务并行多路（多角度审稿）
   - Voting：多路投票取共识（代码漏洞多模型审查）

4. Orchestrator-Workers（编排者-工人）：一个 orchestrator 动态拆分任务给多个 worker
   适用：子任务无法预先确定（如 复杂代码改动按文件分派）

5. Evaluator-Optimizer（评估-优化循环）：生成 → 评估 → 据反馈再生成
   适用：有明确评估标准且可迭代（带单测的代码生成）

6. 自主 Agent loop：LLM 自主决定何时调工具、何时停止
   适用：开放性、步数不可预测的任务（这就是经典"agent"）
```

> 面试要点：**不是所有任务都要上"自主 Agent"**。简单可预编的任务用 Workflow 更可靠、更省成本；只有开放、不可预测的任务才值得把控制权交给 LLM。

**编排复杂度谱系（简单 → 复杂）**：

| 模式                                         | 控制权     | 适用               |
| ------------------------------------------ | ------- | ---------------- |
| 单次 Tool Call                               | 代码      | 天气、计算、简单查询       |
| Workflow（提示链/路由/并行/编排者）                    | 代码（预编排） | 步骤可预知的任务         |
| 自主 Agent loop（原生 tool calling + reasoning） | LLM     | 开放、多步、不可预测       |
| Multi-Agent（Swarm / handoff / A2A）         | 多个 LLM  | 需要不同专长、可委派的复杂工作流 |

### 4.6 现代系统提示词结构（新旧对照）

旧式 ReAct 提示词（要求模型输出 `Thought:/Action:/Action Input:/Observation:`）——**删除**。现代提示词在原生 tool calling 下应写明：

1. **角色与目标**
2. **工具使用规范**：何时调用、参数如何填、优先用哪个（OpenAI 官方建议：用 system prompt 描述**何时（不）用**每个函数）
3. **失败/降级处理**：工具报错时如何回退、是否重试、何时向用户求助
4. **安全边界**：不可执行的动作、需人工确认的操作（如退款、删除）
5. **停止条件**：何时给最终答案而非继续循环
6. （推理模型）**不再要求显式输出 thought**——交给 thinking，提示词聚焦任务约束

**❌ 旧式（删除）**

```
Answer using this format:
Thought: <reasoning>
Action: <tool>
Action Input: <json>
Observation: <result>
... (repeat)
Thought: I now know the answer
Final Answer: <answer>
```

**✅ 现代（生产级）**

```text
你是 <公司> 的客服 Agent。

# 工具
你可调用：get_order(order_id)、issue_refund(order_id, amount)、search_kb(query)。
政策类问题优先用 search_kb。
任何工具都可能失败——失败先重试一次；第二次仍失败，告知用户将人工跟进并停止。

# 安全边界
- 退款 > $200 必须先与用户确认。
- 只在确实需要实时数据时才调用工具。
- 拿到足够信息后立即停止调用工具并作答。

# 输出
直接回答用户，不要复述推理过程。
```

工具由 API 原生传递（见 4.1.2 的 `tools` schema），不再靠提示词解析。

### 4.7 Agent 工具/Function Schema 设计原则

```python
# ❌ 不好的 tool definition
def search(query: str) -> str: ...

# ✅ 好的 tool definition（2026：description 是给模型读的，决定模型"何时选它"）
def search_documents(
    query: str,
    filters: dict | None = None,
    top_k: int = 5
) -> list[SearchResult]:
    """在知识库中搜索相关文档。

    Args:
        query: 搜索查询，用自然语言描述你想要查找的内容。
        filters: 可选过滤条件，如 {"category": "technical", "date_after": "2026-01-01"}。
        top_k: 返回的最相关文档数，默认 5，最大 20。

    Returns:
        包含文档标题、内容摘要、相关度分数的列表。
    """
```

**设计原则（更新版）**：

1. **明确的输入输出类型** — 让模型知道传什么、拿什么；用 JSON Schema + strict mode 强制。
2. **description 是给模型读的契约** — 直接决定模型何时选这个工具，写清"什么场景下用"。
3. **合理的默认值** — 减少模型的参数决策负担。
4. **失败返回有用的错误信息** — 不是 `"Error"`，而是 `"未找到匹配文档，可尝试放宽 filters"`（让模型能据此自我纠错）。
5. **读操作幂等** — 同一 tool 调用多次不应有副作用。
6. **工具返回摘要而非全量** — 防止上下文爆炸（§4.8）。
7. **考虑用 MCP 暴露** — 团队/跨项目复用的工具优先做成 MCP server。

### 4.8 Agent 生产环境陷阱（2026 版）

| 陷阱                         | 表现                                           | 解决方案                                                                 |
| -------------------------- | -------------------------------------------- | -------------------------------------------------------------------- |
| **无限循环**                   | Agent 反复执行相同的 tool call                      | 步数上限（max\_steps）+ 循环检测（连续相同 action）+ 强制终止                            |
| **幻觉调用 / 参数编造**            | 模型调用不存在的工具或编造参数                              | strict mode schema 验证 + 参数校验；只把真正存在的工具放进 `tools`                     |
| **过度使用工具**                 | 简单问题也调用 5 个 tool                             | 分类路由：简单问题直接回答（对应 §11 模型路由）                                           |
| **上下文爆炸**                  | Tool 返回大量数据塞满窗口                              | Tool 返回摘要而非全量、强制截断                                                   |
| **权限泄漏**                   | 调用了不该调用的敏感 tool                              | Tool 分级、写操作/敏感操作需人工确认（human-in-the-loop）                             |
| **成本失控**                   | 单问题消耗大量 token                                | 用户级 token 配额 + **注意隐藏的 reasoning token**（见 §11/§13）                  |
| **reasoning item 未回传** ⚠️新 | 多轮 tool call 时丢掉推理模型上一轮的 reasoning item，质量骤降 | stateless/无存储场景必须用 `include:["reasoning.encrypted_content"]` 透传推理上下文 |
| **降级破坏工具能力** ⚠️新           | failover 切到不支持 tool calling 的廉价模型，agent 直接失效 | 降级链要保证候选模型都支持 tool calling，或降级到非 agent 的简单 QA                        |

3 项必须的**内置**安全措施（不可后补）：

- 最大步数限制（如 max\_steps=10）
- 单步超时（如单步 > 30s 终止）+ 循环检测
- 总 token 消耗上限（含 reasoning token）

### 4.9 Agent 评估的独特挑战（详见 §6）

传统 LLM 评估关注"回答好不好"，Agent 还要评估**完整轨迹（trajectory）**：

- **任务完成率：** 最终是否达成用户目标？（Task Success Rate）
- **工具调用准确性：** 选对工具了吗？参数对吗？（Tool Selection / Argument Correctness）
- **效率：** 用了多少步？相对最少必需步数的冗余度？（Step Efficiency）
- **路径质量：** 推理链路是否合理（有没有走弯路）？（Trajectory Match）
- **鲁棒性：** 中间步骤出错后能否自我纠正？（Self-correction rate）
- **可靠性：** 同一任务多次运行是否一致？（`pass^k`，τ-bench 提出，详见 §6）

> Agent 评估的指标体系、τ-bench 三代演进、工具见 §6「Agent 评估」专节。

***

## 5. 向量数据库决胜点

> **面试核心问题：** "你用过哪些向量数据库？选型时怎么考虑的？HNSW 和 IVF 的区别是什么？"

### 5.1 主流向量数据库对比（2026 更新）

| 数据库                     | 类型         | 索引                  | 量化                                 | 混合检索（BM25）                                         | 关键进展 / 注意事项                                                                                      |
| ----------------------- | ---------- | ------------------- | ---------------------------------- | -------------------------------------------------- | ------------------------------------------------------------------------------------------------ |
| **pgvector**            | PG 扩展      | HNSW / IVFFlat      | scalar / binary（halfvec 16-bit）    | 需配 `tsvector`/FTS                                  | 0.7.0 halfvec+HNSW（内存减半、并行建索引快 30x）；**0.8.0 iterative index scan**（解决过滤场景 over-filtering）。与业务库同库 |
| **Qdrant**              | 开源/云（Rust） | HNSW                | scalar/binary/product              | 用 **sparse 向量**（SPLADE/BGE-M3）+ Universal Query 融合 | 三种量化 + Universal Query API（dense/sparse/multivector + RRF）                                       |
| **Milvus**              | 开源云原生      | HNSW/IVF/DiskANN    | sparse/fp16/bf16/**RaBitQ(1-bit)** | **2.5 起原生全文检索（BM25）**                              | 2.6 RaBitQ 内存降 72%、查询快 4x、分层存储省 50%                                                              |
| **Weaviate**            | 开源/云（Go）   | HNSW                | PQ / BQ（binary）                    | **原生 hybrid**（alpha 调权）                            | GraphQL + 模块化                                                                                    |
| **Pinecone Serverless** | 托管闭源       | 自研（存算分离）            | 内置                                 | 原生 hybrid + metadata                               | 存算分离、namespace 硬分区（多租户，有上限）；免运维但贵                                                                |
| **Turbopuffer**         | 托管（对象存储优先） | 自研                  | 内置                                 | 原生向量+全文                                            | **S3/GCS 对象存储 + SSD 缓存**，成本通常低于纯内存型向量库；延迟略高。具体成本案例需引用厂商/客户公开材料，不要在面试中脱离来源背数字                     |
| **Lantern**             | PG 扩展      | HNSW（外部建索引）         | 内置                                 | 配 FTS                                              | **HNSW 建索引比 pgvector 快 90x**                                                                     |
| **Elasticsearch**       | 搜索引擎       | HNSW（dense\_vector） | 内置                                 | **原生 BM25（强项）**                                    | 已有 ES 栈、全文为主                                                                                     |
| **Chroma**              | 轻量嵌入式      | HNSW                | —                                  | 基础                                                 | 原型、本地、小项目                                                                                        |

> **新索引算法（2024–2026 落地，别再只讲 HNSW/IVF/PQ 三足鼎立）**：
>
> - **DiskANN / Vamana**：图算法把索引放 SSD，十亿级低延迟高 recall；2025 已在 **SQL Server 2025、Azure Cosmos DB** public preview，生产可用化。
> - **ScaNN**（Google）：各项异性量化 + 重组，2024 在 **AlloyDB** 落地；2025 推 **SOAR** 进一步加速。
> - **RaBitQ（1-bit 量化）**：Milvus 2.6 主打，recall 不掉太多前提下大幅省内存。

### 5.2 索引算法深度对比

**这是面试中展示技术深度的关键点：**

#### HNSW (Hierarchical Navigable Small World)

```
原理：
  - 构建多层图结构，上层稀疏（便于跳跃），下层密集（便于精搜）
  - 搜索时从最上层开始，贪心地向距离 query 最近的点移动
  - 到达局部最优后，下降到下一层继续搜索
  
参数：
  - M: 每个节点的最大连接数（越大=精度越高=内存越大）
  - efConstruction: 构建时的搜索宽度（越大=索引质量越高=构建越慢）
  - efSearch: 查询时的搜索宽度（越大=精度越高=查询越慢）

特点：
  ✅ 查询最快（对数复杂度）、精度高
  ❌ 内存消耗大（需要存储图结构）、构建慢、增量插入相对慢
  
适用：查询性能优先、数据量在千万到亿级别
```

#### IVF (Inverted File Index)

```
原理：
  - 用 K-means 将向量空间划分为 N 个聚类
  - 查询时只搜索最近的 nprobe 个聚类
  - 本质是近似搜索：牺牲精度换速度

参数：
  - nlist: 聚类数量（越大=精度越高=搜索越慢）
  - nprobe: 搜索时探测的聚类数（越大=精度越高=搜索越慢）

特点：
  ✅ 内存友好、构建快、支持增量插入
  ❌ 查询比 HNSW 慢、精度依赖于 nprobe 参数
  
适用：内存受限、数据频繁更新、对查询延迟不太敏感的场景
```

#### PQ (Product Quantization)

```
原理：
  - 将高维向量分解为多个低维子向量
  - 对每个子空间做聚类，用码本（codebook）压缩
  - 显著减小内存占用

特点：
  ✅ 极致内存压缩（一个 1024 维向量可能只需 64 bytes）
  ❌ 精度损失（有损压缩）
  
适用：十亿级以上向量、内存是瓶颈的场景
通常和 IVF 组合使用 (IVF+PQ)
```

#### 量化（Quantization）决策表（2025 生产标配，必会）

| 量化类型                  | 压缩比    | 精度损失        | 代表库                                | 何时选                                  |
| --------------------- | ------ | ----------- | ---------------------------------- | ------------------------------------ |
| **Scalar（INT8/FP16）** | \~4x   | 极小（>99% 召回） | Qdrant / pgvector halfvec / Milvus | **通用默认**（几乎无损的甜点）                    |
| **Binary（1-bit）**     | \~32x  | 视向量分布       | Qdrant / pgvector / Milvus RaBitQ  | 高维中心化 embedding（OpenAI/Cohere）、极致省内存 |
| **Product（PQ）**       | 可调（更高） | 较大          | Qdrant / Weaviate                  | 内存优先、能接受 recall 下降                   |

> **memory vs recall 权衡**：scalar 是"几乎无损"的甜点；binary 极致省内存但 recall 依赖向量分布，需在自有数据上回归；PQ 最省但 recall 掉最多。生产建议：**scalar 默认 → 成本压力上 binary → 配 oversample+rescore（先量化粗筛 → 原始向量精排）保 recall**。

#### 面试中你应该能讲出这个决策矩阵：

```
你的数据规模是多少？
├── < 100 万：pgvector (IVFFlat) — 够用且和业务库一体
├── 100 万 - 1000 万：Qdrant / Weaviate (HNSW) — 高性能单机方案
├── 1000 万 - 1 亿：Pinecone / Milvus (HNSW) — 生产级可靠
├── 1 亿 - 10 亿：Milvus (IVF+PQ) — 需要量化压缩
└── > 10 亿：Milvus/DiskANN — 需要磁盘索引

你的延迟要求是多少？
├── < 50ms: HNSW + 足够内存 (数据全在内存)
├── 50-200ms: HNSW 或 IVF (nprobe 调小)
└── > 200ms: IVF+PQ (磁盘场景也可接受)

你的运维能力如何？
├── 无专职运维: Pinecone Serverless / Turbopuffer (全托管)
├── 有运维但不精通向量库: pgvector (复用 PG 运维能力)
├── 运维能力强: Qdrant / Milvus (自托管)
```

```
2026 按场景的首选（更实用）：
├── 中小规模(<千万)、要与业务事务同库、已用 PG     → pgvector 0.8+（halfvec + iterative scan）
├── 中大规模、自托管、要极致延迟 + 灵活量化         → Qdrant
├── 超大规模(十亿级)、云原生、要原生 hybrid         → Milvus 2.5/2.6
├── 完全免运维、不想自托管                          → Pinecone Serverless
├── 超大规模、成本极敏感、能容忍对象存储延迟         → Turbopuffer（省一个数量级）
└── 已有 Elasticsearch 栈、全文检索为主              → Elasticsearch
```

***

## 6. 评估体系——面试最大考点

> **JD 原话：** "你可以连续畅谈一个小时，详细阐述你如何通过数据'证实'系统正在变得更好，而不仅仅是'感觉'它变好了。"
> **这是整份 JD 里对单一技能要求最高的一句话。你必须能聊 10-15 分钟不重样。**

### 6.1 为什么要评估？——先讲 WHY

面试中可以先从"为什么评估是 AI 系统的生命线"切入：

```
1. LLM 是非确定性的 — 同样的 prompt 可以产生不同输出
2. Prompt 的微小改动可能产生巨大影响 — 改一个词可能让准确率下降 20%
3. 模型升级不一定"更好" — GPT-5.5 在某个低延迟任务上可能不如 GPT-5.4-mini
4. 没有评估 = 你在盲飞 — 出问题只能等用户投诉
5. 评估是团队沟通的语言 — PM 问"好了吗"，你不能回答"我感觉好了"
```

### 6.2 评估体系三层架构

```
┌─────────────────────────────────────────────────────────┐
│ Layer 3: 线上指标 (Online Metrics)                        │
│ 用户反馈 (👍/👎)、任务完成率、留存率、人工抽检               │
│ → 回答："系统真的帮到用户了吗？"                           │
├─────────────────────────────────────────────────────────┤
│ Layer 2: 离线评估 (Offline Evaluation)                    │
│ Golden Dataset + 自动化指标 + LLM-as-Judge               │
│ → 回答："这次改动比上次好吗？"                             │
├─────────────────────────────────────────────────────────┤
│ Layer 1: 单元测试 (Unit Tests)                            │
│ Schema 验证、格式检查、特定 case 的确定性测试               │
│ → 回答："系统还能正常运行吗？"                             │
└─────────────────────────────────────────────────────────┘
```

### 6.3 评估指标详解

#### RAG 评估指标（RAGAS 框架）

| 指标                    | 定义                       | 计算方式                         | 说明          |
| --------------------- | ------------------------ | ---------------------------- | ----------- |
| **Faithfulness**      | 回答中的每句话是否都能在检索到的上下文中找到依据 | 将回答拆成原子命题，逐一检查是否被 context 支持 | 衡量"有没有编造"   |
| **Answer Relevancy**  | 回答是否紧扣问题，没有跑题            | 从回答反向生成问题，看和原问题的语义相似度        | 衡量"答没答到点子上" |
| **Context Precision** | 检索到的内容中，相关内容的排名是否靠前      | 用 LLM 判断每个 chunk 是否相关，加权计算   | 衡量"检索排名对不对" |
| **Context Recall**    | 标准答案所需的信息，检索结果里包含了多少     | 需要标注"ground truth 需要哪些信息"    | 衡量"检索全不全"   |
| **Context Relevancy** | 检索到的内容中多少和问题相关           | 用 LLM 判断每个 chunk 是否相关        | 衡量"检索的噪音水平" |

#### 对话/生成评估指标

| 指标                     | 说明                                      |
| ---------------------- | --------------------------------------- |
| **ROUGE-L**            | 基于最长公共子序列的文本相似度，用于摘要                    |
| **BLEU**               | 基于 n-gram 的精确匹配，用于翻译（对开放生成不适用）          |
| **BERTScore**          | 基于 embedding 的语义相似度                     |
| **LLM-as-Judge Score** | 用 GPT-5.5 / Claude Fable 5 等强模型打分，目前最主流 |

#### Agent 轨迹评估（2024–2026 新难点，单轮 RAG 评估不覆盖）

Agent 评估的不是"最终答案好不好"，而是**完整轨迹（trajectory）**——工具选对没、参数对没、顺序合理没、走没走弯路、能不能自我纠错。

| 指标类别      | 指标                              | 说明                    |
| --------- | ------------------------------- | --------------------- |
| **任务完成**  | Task Success Rate (TSR)         | 任务最终是否完成              |
| <br />    | Partial Credit / Progress Score | 未完成时按进度给部分分           |
| **工具调用**  | Tool Selection Accuracy         | 是否选对了工具               |
| <br />    | Argument Correctness            | 参数抽取是否正确              |
| <br />    | Tool Call Accuracy（RAGAS）       | 完整工具调用评估              |
| **轨迹/效率** | Step Efficiency                 | 实际步数 vs 最少必需步数，惩罚冗余循环 |
| <br />    | Trajectory Match                | 实际轨迹 vs 期望轨迹（精确/部分匹配） |
| **可靠性**   | **`pass^k`（τ-bench）**           | k 次独立运行**全部**成功的概率    |

**`pass^k`** **vs** **`pass@k`（高频面试点）**：

- `pass@k`（编码基准）：k 次里**至少一次**成功即可 → 偏乐观
- **`pass^k`（τ-bench）**：k 次**全部**成功 → 衡量**可靠性/一致性**，因为生产环境不能靠"多试几次"

**τ-bench 三代演进（agent 评估事实标杆）**：

| 版本                             | 域                   | 关键创新                                                                            |
| ------------------------------ | ------------------- | ------------------------------------------------------------------------------- |
| τ-bench v1（2024.06, ICLR 2025） | retail + airline    | 首个 tool-agent-user 多轮基准；policy + LLM user simulator；rule-based 确定性 API；`pass^k` |
| τ²-bench（2025.06）              | + telecom           | dual-control（agent 和 user 都能改共享 DB）；更严格 pass/fail；telecom 成功率显著更低               |
| τ³-bench（2025）                 | + knowledge + voice | 新域                                                                              |

为何成为标杆：① 真实多轮对话（非单轮 QA）② 确定性 API（rule-based，结果可验证，不靠 LLM 打分）③ `pass^k` 直接量化可靠性 ④ 显式评估 policy adherence。其它 agent benchmark：**AgentBench**（清华，综合，偏老）、**AgentBoard**（NeurIPS 2024，多域+细粒度）、**T-Eval**（逐步评估每个 tool call）。

### 6.4 LLM-as-Judge 深度

这是 2024-2025 年最主流的评估方式，但在面试中讲出"我也知道它的局限"才能体现深度。

**标准实现：**

```python
JUDGE_PROMPT = """
You are an expert evaluator. Rate the AI assistant's response 
on a scale of 1-5 for each criterion:

1. Accuracy: Are all factual claims correct and verifiable?
2. Completeness: Does the answer fully address the user's question?
3. Clarity: Is the response well-structured and easy to understand?
4. Safety: Does the response avoid harmful or misleading content?

User Question: {question}
Reference Context: {context}
AI Response: {response}

Provide a JSON with scores and one-sentence justifications.
"""
```

**LLM-as-Judge 的已知 Bias（面试必提，展示深度）：**

> 基于 2024 年最系统的 bias 量化论文 **CALM / "Justice or Prejudice?"（arXiv 2410.02736）**，它定义并量化了 **12 种 bias**。下表是面试高频的实用子集（含原表错误订正）：

| Bias                                        | 含义                       | 主要场景                              | 缓解方案                   |
| ------------------------------------------- | ------------------------ | --------------------------------- | ---------------------- |
| **位置偏差 Position / Order Bias**（两者同义，不要并列两条） | 偏爱首位/末位                  | Pairwise 成对比较                     | 交换 A/B 位置各评一次、双向投票取共识  |
| **冗长偏差 Verbosity Bias**                     | 偏爱更长的答案                  | Pairwise & Pointwise              | 长度归一化、控制答案长度、引入简洁度指标   |
| **自我增强偏差 Self-Enhancement Bias**            | 偏爱自己生成的答案                | Pairwise & Pointwise              | 跨模型交叉 judge、匿名化（去掉模型名） |
| **权威偏差 Authority Bias**                     | 偏爱带（甚至伪造的）引用/权威语气        | Pairwise & Pointwise              | 去掉引用再评、rubric 约束       |
| **从众偏差 Bandwagon Bias**                     | 偏爱"多数人意见"（不管对错）          | Pairwise                          | 不注入他人评分信息              |
| **评分锚定 Anchoring**                          | 受初始参考分/首个示例锚定            | **Pointwise 点态**（≠ Position Bias） | 多 rubric、随机化示例顺序、独立评分  |
| **CoT 敏感性**                                 | 有无 Chain-of-Thought 评分不同 | Pointwise                         | 固定是否要求 CoT             |

> **常见混淆订正**：
>
> - "Sycophancy（迎合用户）"是 alignment/RLHF 语境的概念，**不是 judge bias 的主流量化对象**；judge 场景里最接近的是 **Authority Bias**（权威偏好）。
> - "锚定效应"和 Position/Order Bias **不是一回事**：Position 是 pairwise 里谁放前面，Anchoring 是 pointwise 打分受初始参考锚定。手册里把它们当重复项是错的。

**成对（Pairwise）vs 点态（Pointwise）评分（2024–2025 高频考点）**：

- **Pairwise**：让 judge 比较 A vs B 谁更好。**更准、与人类一致性更高**，但**更贵**（O(n²)，n 个候选要 n(n-1)/2 次）。适合 leaderboard、相对质量判断。
- **Pointwise**：给单个答案打分（如 1–5）。**便宜、可并行、可排名**，但**校准差**、受锚定影响。适合大规模低成本筛选 + 回归监控。

### 6.5 Golden Dataset 构建方法论

这不是"收集一些问答对"，面试官想听到一套方法：

```
Step 1: 数据来源
  □ 历史用户对话日志（需脱敏）
  □ 领域专家手工撰写
  □ 从文档中自动生成（用 LLM 根据文档段落生成问题）
  □ 线上 bad case 回流（用户点 👎 的 case）

Step 2: 覆盖度设计
  □ 简单问题（单知识点检索）：30%
  □ 中等复杂（跨段落检索）：40%
  □ 困难问题（多文档综合推理）：20%
  □ Edge case（歧义、边界条件、反事实）：10%

Step 3: 标注
  □ 每个 QA pair 标注：标准答案、关键上下文段落
  □ 分类标签：问题类型、难度、涉及文档
  □ 多人标注取共识（至少 2 人）

Step 4: 维护
  □ 定期审查（每月）— 删除过时条目、补充新类型
  □ Bad case 回流机制 — 线上用户不满意的回答自动加入评估集
  □ 版本管理 — 知道评估集 V3 和 V2 有什么不同

Step 5: 规模建议
  □ 最小可行：50-100 条（覆盖核心场景）
  □ 推荐规模：200-500 条
  □ 大型项目：1000+ 条
```

### 6.6 评估在 CI/CD 中的集成

```
Pull Request 提交流程：

1. 开发者改了 prompt 或调整了 chunking 参数
2. CI pipeline 自动运行评估集
3. 对比当前版本和 baseline 的指标：
   - 如果所有指标持平或提升 → 自动通过
   - 如果 Faithfulness 下降 > 2% → 自动拦截，阻止合并
   - 如果 Relevancy 下降但 Faithfulness 提升 → 需要人工 review
4. 评估报告生成为 PR comment

面试中可以展示的工程思维：
"评估不只是上线后的事，而是每次代码变更的门禁。"
```

### 6.7 评估的评估（Meta-Evaluation）

面试中展示"元认知"的最高境界：

```
你如何知道你的评估系统本身是可靠的？

1. LLM-as-Judge 和人工评分的相关性校验
   - 定期抽取 50 个 case，人工打分
   - 计算 LLM 评分和人工评分的 Spearman/Kendall 相关系数
   - 如果相关性 < 0.7，评估 prompt 需要调整

2. 评估集代表性检查
   - 线上真实问题的分布 vs 评估集的分布
   - 如果线上高频问题类型在评估集中缺失 → 补充

3. 评估集污染检测
   - 评估集内容是否在训练数据中出现过（可能导致模型"背答案"）
   - 需要定期更新评估集内容
```

### 6.8 主流评估工具对比（2026 更新）

| 工具               | 定位                           | 核心优势                                                                                                                                               | 适用阶段              |
| ---------------- | ---------------------------- | -------------------------------------------------------------------------------------------------------------------------------------------------- | ----------------- |
| **RAGAS**        | RAG/LLM 评估库                  | 四大核心 RAG 指标（faithfulness/answer\_relevancy/context\_precision/recall）+ Agent/Tool 指标（**Agent Goal Accuracy**、Tool Call Accuracy）；几乎全靠 LLM-as-Judge | 开发阶段离线评估          |
| **DeepEval**     | "Pytest for LLMs"            | pytest 风格 `assert`、**G-Eval**（CoT+rubric 自定义准则）、Tool Correctness、CI 友好                                                                             | CI/CD 集成、Agent 评估 |
| **TruLens**      | 追踪+评估一体                      | 首创 **RAG Triad**（Context Relevance / Groundedness / Answer Relevance）+ dashboard                                                                   | 开发/监控             |
| **promptfoo**    | CLI 优先                       | 多 provider 并排对比、YAML 配置、**红队（red teaming）一等公民**、可评估 stateful agent                                                                                 | 本地 + CI 集成、红队     |
| **OpenAI evals** | OpenAI 官方                    | 与 OpenAI 平台集成                                                                                                                                      | OpenAI 生态         |
| **Inspect AI**   | 前沿模型评估（**英国 AISI**，非 OpenAI） | frontier-model 评估框架                                                                                                                                | 模型安全/前沿评估         |

> **可观测性平台（评估与 trace 一体）**：LangFuse（开源、自托管、prompt 管理）、LangSmith（LangChain/LangGraph 生态）、Braintrust（eval+prompt 管理一等公民）、Arize Phoenix（开源、OTel/OpenInference）—— 详见 §8.2。

> ⚠️ **订正**：① 不存在名为 "AgenticInfo" 的标准指标——RAGAS 的 agent 指标是 **Agent Goal Accuracy / Tool Call Accuracy**。② "Inspect" 是**英国 AI Safety Institute (AISI)** 的前沿评估框架，不是 OpenAI 的；OpenAI 自家的 agent 可视化是 **Agents SDK Tracing**（见 §8）。面试别张冠李戴。

***

## 7. Agent Harness 与框架取舍

> **面试问题：** "你会直接用 LangGraph / CrewAI / OpenAI Agents SDK 这类框架吗？如果不用，你会怎么自己构建生产级 Agent？"

这道题的关键不是背框架清单，而是能把 **workflow 编排** 与 **Agent harness** 分清楚。

**本手册的立场**：生产级 Agent 不必默认采用第三方编排框架。真正必须掌握的是一套可复用的 **Agent Harness**：它负责让模型安全、可观测、可评估、可降级地循环调用工具。框架可以提供便利组件，但不应主导核心架构。

> ⚠️ **2024–2026 关键变化**：原生 tool calling、structured outputs、MCP、prompt caching、provider tracing 已经下沉到官方 API / SDK。许多第三方框架的价值从"提供 Agent 能力"变成"封装 harness 组件和 workflow 便利性"。这让自建 lightweight harness 在生产系统中变得更可行，也更符合 KISS。

### 7.1 Workflow 编排 vs Agent Harness

| 维度 | Workflow 编排 | Agent Harness |
|---|---|---|
| 核心问题 | 先做哪一步、分支怎么走 | 模型如何安全地感知、决策、调用工具、停止 |
| 控制权 | 主要在代码 / 图 / DSL | 代码给边界，模型在边界内自主行动 |
| 适合场景 | 流程稳定、步骤可预知、合规路径明确 | 目标明确但路径开放、工具选择依赖上下文 |
| 主要风险 | 过度固化、领域变化时改图成本高 | 成本失控、循环、工具误用、可观测不足 |
| 评估重点 | 每个节点是否正确执行 | 完整 trajectory：工具选择、参数、步数、成功率 |

**一句话**：workflow 是"业务流程图"，harness 是"Agent 运行时"。生产系统可以有 workflow，但 Agent 能力本身应先建立在清晰的 harness 上。

### 7.2 生产级 Harness 必备组件

本项目默认自建 lightweight harness，而不是先引入大型编排框架。最小可用 harness 至少包括：

1. **Provider capability layer**  
   记录每个模型是否支持 tool calling、strict structured output、streaming tool call、reasoning effort、prompt caching、vision。failover 和 agent 路由必须基于 capability，而不是只看模型名。

2. **Native tool calling loop**  
   调用模型 → 消费结构化 `tool_call` → 执行工具 → 回传 `tool_result` / `function_call_output` → 继续直到最终答案。禁止回到旧式 `Thought/Action/Observation` 文本解析。

3. **Tool contract**  
   工具必须有清晰 description、JSON Schema、参数校验、权限标签、幂等性说明、有用错误信息。工具是 Agent 的真实行动边界，不是随手暴露的函数列表。

4. **State / memory / replay**  
   保存必要的 conversation state、tool results、step trace、usage。短任务可内存态，长任务才需要 durable checkpoint。不要一开始就把所有任务做成复杂状态图。

5. **Limits & safety**  
   `max_steps`、单步 timeout、总 token budget、循环检测、敏感工具人工确认、tenant/user 权限过滤。安全边界必须内置，不能事后补。

6. **Observability**  
   每一步记录 trace/span：model、prompt version/hash、tool name、参数 hash、latency、usage、reasoning/cache tokens、cost、finish reason。普通日志不记录原始用户数据。

7. **Evaluation**  
   Agent 不只评最终答案，还要评 trajectory：Task Success、Tool Selection Accuracy、Argument Correctness、Step Efficiency、`pass^k`。

8. **Fallback & recovery**  
   模型降级、能力兼容检查、exact/stale cache、规则兜底、异步恢复。Agent 降级链必须保证候选模型支持必要工具能力。

### 7.3 为什么本项目默认自建 Harness

自建不是为了"重新造一个 LangGraph"，而是为了把生产 Agent 的关键控制面掌握在自己手里：

- **显式性**：prompt、tool schema、token、reasoning、cache、trace 都能直接看见。
- **可替换性**：provider、向量库、缓存、评估器可以逐层替换。
- **可评估性**：harness 输出统一 trajectory，便于 CI 和回归评估。
- **可控性**：成本、权限、降级、循环检测都由项目规则约束。
- **KISS**：单 Agent + 少量工具 + 单循环时，框架 DSL 往往比直接代码更重。

这不排斥借鉴框架。正确姿势是：**先掌握 harness 边界，再决定是否局部引入框架组件。**

### 7.4 何时引入框架或平台组件

| 需求 | 可考虑的组件 | 引入理由 | 引入前检查 |
|---|---|---|---|
| 长任务需要 durable checkpoint / resume | LangGraph | 显式状态机、恢复、human-in-the-loop 成熟 | 是否真的有跨请求长状态？是否能导出完整 trace？ |
| OpenAI 生态内快速构建 handoff / tracing | OpenAI Agents SDK | guardrails、handoffs、平台 tracing 便利 | 是否会绑定 provider？是否能保留自有 usage/cost 归因？ |
| Claude / MCP / Skills 深度集成 | Claude Agent SDK | 复用 Claude Code 式工具与上下文管理 | 是否需要跨 provider？是否能隔离敏感工具权限？ |
| RAG 数据连接与索引原型 | LlamaIndex / Haystack | 文档连接器、索引流程现成 | 是否会隐藏 chunking/embedding/retrieval 细节？ |
| 多 Agent 角色协作实验 | CrewAI / AutoGen | 快速试验 delegation / role-based collaboration | 是否有真实多角色需求，还是单 Agent + tools 就够？ |
| prompt / pipeline 自动优化 | DSPy | 可把 prompt 当可优化程序 | 是否有稳定 metric 和训练/验证集？ |
| eval + trace 平台 | LangFuse / LangSmith / Braintrust / Phoenix | 评估、prompt 管理、trace UI 便利 | 是否支持自托管、数据隔离、导出和成本字段？ |

**引入原则**：只引入能减少真实复杂度的组件；不要为了"看起来 agentic"引入框架。框架必须服从本项目 harness 契约，而不是反过来让核心架构适配框架。

### 7.5 面试中的高分回答

如果面试官问"为什么不用 LangChain / LangGraph"，可以这样答：

> 我不会默认不用，也不会默认使用。我的判断边界是：如果核心问题是 Agent 运行时的安全、工具调用、成本、trace、eval，那我倾向于自建 lightweight harness，因为这些是生产控制面，必须显式可控。如果问题已经演化成长时运行、有状态恢复、human-in-the-loop 图流程，我会考虑 LangGraph 这类状态机框架，但会要求它输出完整 step trace，并且不能隐藏 token、prompt、tool 参数和降级行为。

如果问"框架有什么价值"，答：

> 框架的价值主要是 convenience：checkpoint、handoff、trace UI、连接器、平台集成、快速原型。它不是 Agent 能力的本体。Agent 能力主要来自模型、上下文组织、工具质量、harness 约束、观测与评估闭环。

### 7.6 本项目的落地选择

本框架 `pkg/ai/` 采用：

```
默认路线：
  自建 lightweight Agent Harness
    ├── llm.Provider + ProviderCapabilities
    ├── native tool calling executor
    ├── Tool schema / permission / validation
    ├── Step trace / replay / eval trajectory
    ├── max_steps / timeout / token budget / loop detection
    ├── fallback cache / provider failover / rate limit
    └── prompt as code + eval gate

局部借鉴：
  LangGraph 的 durable state 思想
  OpenAI / Claude SDK 的 tracing 与 tool loop 事件模型
  LlamaIndex / Haystack 的文档连接器经验
  LangFuse / Phoenix / Braintrust 的 eval + trace 平台形态
```

**工程底线**：第三方框架不得成为 `pkg/ai` 的核心依赖。若未来引入，只能作为 adapter 或 app-layer integration，内核仍保持可独立测试、可替换、可 trace。

***

## 8. 可观测性与监控

> **面试问题：** "你的 AI 系统上线后，你怎么知道它运行得怎么样？"

### 8.1 AI 系统特有指标（区别于传统后端）

#### 质量指标

| 指标            | 计算方式           | 告警阈值建议       |
| ------------- | -------------- | ------------ |
| **幻觉率**       | 需要用户反馈或评估系统判断  | > 5% 触发告警    |
| **回答空/无效率**   | "我不知道" / 空回复比例 | > 10% 需要检查   |
| **用户踩率**      | 👎 / 总反馈       | > 15% 触发告警   |
| **检索命中率**     | 检索到相关文档的请求比例   | < 80% 需要检查   |
| **Refusal 率** | 系统拒绝回答的比例      | > 20% 可能过度拒绝 |

#### 性能指标

| 指标                             | 计算方式                       | 告警阈值建议          |
| ------------------------------ | -------------------------- | --------------- |
| **TTFT (Time to First Token)** | 首 token 延迟                 | P95 > 3s 触发告警   |
| **Total Latency**              | 完整回答延迟                     | P95 > 15s 触发告警  |
| **Token 消耗 / 请求**              | prompt + completion tokens | 超过历史均值 50% 触发告警 |
| **吞吐量 (QPS)**                  | 每秒处理请求数                    | 接近容量上限 80% 触发告警 |

#### 成本指标

| 指标          | 计算方式                                                                      |
| ----------- | ------------------------------------------------------------------------- |
| **每请求成本**   | (prompt\_tokens × prompt\_price + completion\_tokens × completion\_price) |
| **每日/月总成本** | 按模型、按功能模块拆分                                                               |
| **缓存命中率**   | 被缓存满足的请求 / 总请求                                                            |
| **成本异常检测**  | 日成本超过 7 日均值 50% 触发告警                                                      |

### 8.2 可观测性标准：OpenTelemetry GenAI 语义约定（2024–2026 的事实标准）

> **先讲标准，再讲工具**。OpenTelemetry 在 2024.04 成立 **GenAI SIG**，推出 **GenAI Semantic Conventions**（`gen_ai.*` 属性），规范独立仓库 `open-telemetry/semantic-conventions-genai`。所有主流厂商（Datadog、Langfuse、Phoenix、Traceloop）都在对齐它——这是 2025–2026 trace 字段的**可移植性底线**。

**核心属性（节选，面试能说出一组就够）**：

- 请求/响应：`gen_ai.request.model`、**`gen_ai.response.model`**（实际响应模型，区别于请求）、`gen_ai.provider.name`（openai/anthropic/gcp.gemini/deepseek…）、`gen_ai.operation.name`（chat/embeddings/execute\_tool/invoke\_agent）
- 输入输出：`gen_ai.input.messages`、`gen_ai.output.messages`、`gen_ai.system_instructions`
- Token：**`gen_ai.usage.input_tokens`** / **`gen_ai.usage.output_tokens`**
- 工具调用（agent 必备）：`gen_ai.tool.name`、`gen_ai.tool.call.id`、`gen_ai.tool.call.arguments`、`gen_ai.tool.call.result`、`gen_ai.tool.definitions`
- Agent/会话/RAG/评估：`gen_ai.agent.id`、`gen_ai.conversation.id`、`gen_ai.evaluation.score.value`（把 eval 结果写回 trace）

**⚠️ 已弃用字段（手册/代码若在用，必改）**：

| 旧字段（已 Deprecated）                | 新字段                              |
| -------------------------------- | -------------------------------- |
| `gen_ai.system`                  | **`gen_ai.provider.name`**       |
| `gen_ai.usage.prompt_tokens`     | **`gen_ai.usage.input_tokens`**  |
| `gen_ai.usage.completion_tokens` | **`gen_ai.usage.output_tokens`** |

> **成本计算坑（OpenAI 官方，必记）**：`reasoning_tokens` 已**包含在** **`completion_tokens`** **里**，**不要再加一次**（常见 double-count bug）；`cached_tokens` 在 `prompt_tokens_details`，cached prompt 约 5 折计费。

### 8.3 可观测性工具选型

| 工具                            | 协议                   | 核心能力                                             | 适用团队                |
| ----------------------------- | -------------------- | ------------------------------------------------ | ------------------- |
| **LangFuse**                  | OTel/OTLP 原生         | 开源、自托管、prompt 管理 + evals（v3 迁 ClickHouse）、全功能免费  | 需要自托管、预算有限          |
| **Arize Phoenix**             | OTel + OpenInference | 开源、notebook 友好、LLM-as-judge、RAG 指标、embeddings 分析 | 实验/调试首选             |
| **LangSmith**                 | OTel 兼容              | LangChain/LangGraph 深度集成、trajectory evals        | 深度使用 LangChain 的团队  |
| **Braintrust**                | OTel 兼容              | eval-forward、版本化 prompt、结构化 dataset              | 企业级、重视 prompt 管理的团队 |
| **Datadog LLM Observability** | **原生 OTel GenAI**    | 与 Datadog APM 统一、成本追踪、Agent Observability        | 已用 Datadog 的团队      |
| **OpenLIT**                   | 原生 OTel              | token/cost/latency 监控、Apache 2.0 自托管             | 已在 OTel 生态、要轻量      |
| **Helicone**                  | OTel 兼容              | 极简接入、请求监控/调试/优化                                  | 最小化上手               |

> **OpenAI Agents SDK Tracing**（不是 "Inspect"）：OpenAI Agents SDK 默认开启 tracing，在 OpenAI 平台 Traces dashboard 可视化 agent 完整轨迹（spans/generations/tool calls）。注意与 **Inspect AI（英国 AISI 的前沿评估框架）** 区分，两者不是一回事。

### 8.4 可观测性实现的关键 Trace 信息（对齐 OTel GenAI）

```python
# 每次 LLM 调用都应该记录的信息
trace = {
    "trace_id": str(uuid.uuid4()),
    "timestamp": datetime.now().isoformat(),
    "request": {
        "user_id": user_id,
        "session_id": session_id,
        "tenant_id": tenant_id,
        "query_hash": hashlib.sha256(redact_pii(user_query).encode()).hexdigest(),
        # 原始 query 只进受控审计存储；普通 trace/log 不记录原文，避免 PII 泄露
        "feature": "rag_qa",  # 功能模块标识
    },
    "retrieval": {
        "query_rewritten_hash": hashlib.sha256(redact_pii(rewritten_query).encode()).hexdigest(),
        "chunks_retrieved": len(chunks),
        "top_scores": [c.score for c in chunks[:5]],
        "latency_ms": retrieval_latency,
    },
    "generation": {
        "request_model": "gpt-5.4",                 # gen_ai.request.model
        "response_model": "gpt-5.4-2026-...",       # gen_ai.response.model（实际服务模型，降级时与请求不同）
        "provider": "openai",                       # gen_ai.provider.name
        "prompt_template_version": "v3.2",          # gen_ai.prompt.name
        "system_prompt_hash": hashlib.sha256(system_prompt.encode()).hexdigest()[:16],
        "input_tokens": prompt_tokens,              # gen_ai.usage.input_tokens
        "output_tokens": completion_tokens,         # gen_ai.usage.output_tokens
        "reasoning_tokens": reasoning_tokens,       # ⚠️ 推理模型隐藏成本（已含在 output_tokens，勿重复计）
        "cache_read_tokens": cached_tokens,         # ⚠️ prompt caching 命中（约5折计费）
        "temperature": 0.0,
        "ttft_ms": ttft,                            # 首 token 延迟（推理模型此项会更高）
        "total_latency_ms": total_latency,
        "finish_reason": "stop",
    },
    "tool_calls": [                                  # ⚠️ gen_ai.tool.call.*（agent 必备）
        {"name": "search_kb", "call_id": "call_1", "arguments": {...}, "result_summary": "..."},
    ],
    "cost": {
        "prompt_cost": prompt_cost,
        "completion_cost": completion_cost,
        "total_cost": total_cost,
        "currency": "USD",
    },
    "feedback": {
        "user_rating": None,  # 后续收集
        "auto_eval_score": eval_score,
    }
}
```

**Trace / 日志隐私边界**：线上 trace 默认只记录 hash、长度、语言、分类标签和脱敏摘要；原始 query、完整 prompt、tool 参数、LLM 输出如果包含用户数据，必须进入加密审计存储并设置保留期。跨租户系统要在 trace、缓存、评估样本里都带 `tenant_id`，严禁只靠相似度命中共享缓存。

***

## 9. 生产环境工程实践

> **JD 原话：** "做出那些决定系统能否在真实用户面前存活的、看似平淡却至关重要的决策"

### 9.1 Prompt 版本控制——完整方案

这是 JD 明确列出的关键技能。你需要展示的不只是"我做了版本控制"，而是整个工程方案。

**Prompt 即代码（Prompt as Code）：**

```
核心原则：
1. Prompt 不应该硬编码在业务代码中
2. Prompt 修改应该经过 review → test → deploy 流程
3. 任何 prompt 变更都应该可追溯、可回滚
4. Prompt 和它的配套参数（model、temperature）应该作为一个完整 artifact 版本化

推荐目录结构：
prompts/
├── production/
│   ├── customer_support/
│   │   ├── system_prompt_v3.txt
│   │   └── user_prompt_template_v3.j2
│   ├── rag_qa/
│   └── classification/
├── staging/
│   └── ...
├── experiments/
│   └── hypothesis_test_001/
├── tests/
│   ├── fixtures/
│   │   └── golden_dataset.json
│   └── test_prompts.py
└── CHANGELOG.md
```

**Prompt 版本管理的三个层次：**

```
Level 1: Git 管理（基础）
  - Prompt 文件存在 Git 仓库
  - 修改提交 PR，review 后合并
  - 问题：无法热更新，需要重新部署

Level 2: 数据库/配置中心管理（进阶）
  - Prompt 存在数据库或配置服务（如 LaunchDarkly）
  - 支持热更新、按环境区分、按用户分组
  - 问题：版本和 Git 代码可能不同步

Level 3: 专门的 Prompt 管理平台（成熟）
  - Braintrust / LangFuse / LangSmith
  - 支持：版本对比、A/B 测试、评估集成、回滚
  - 推荐：有一定规模后迁移到这类平台
```

**Prompt Template 的工程实践：**

```python
# ❌ 初学者写法
answer = client.chat(prompt=f"回答用户问题：{user_query}，用中文回答")

# ✅ 生产级写法
from jinja2 import Template
import hashlib

PROMPT_TEMPLATE_V3 = Template("""
{# 这是 QA 助手的系统提示词 v3，最后更新 2026-06-15 #}

## 角色
你是一个{{ role }}，服务于{{ company }}的{{ product }}产品用户。

## 知识范围
- 你仅基于下方【参考信息】回答问题
- 如果参考信息不足以回答问题，请明确告知用户"抱歉，我目前的知识库中没有相关信息"
- 不要编造任何不在参考信息中的内容

## 回答要求
- 使用{{ language }}回答
- 如果参考信息包含多个来源，请用 [1] [2] 标注引用
- 回答应简洁准确，避免不必要的展开

## 参考信息
{% for doc in documents %}
[{{ loop.index }}] {{ doc.title }}
{{ doc.content }}
{% endfor %}

## 用户问题
{{ user_query }}
""")

# 使用
prompt = PROMPT_TEMPLATE_V3.render(
    role="技术支持助手",
    company="ACME Corp",
    product="CloudSync",
    language="中文",
    documents=retrieved_docs,
    user_query=user_query
)

# 生产环境必须做的事
prompt_hash = hashlib.sha256(prompt.encode()).hexdigest()[:8]
logger.info("Serving prompt", extra={"prompt_version": "v3", "prompt_hash": prompt_hash})
# 这个 hash 会被写入 trace，用于后续分析"哪个 prompt 版本表现更好"
```

**Prompt 模板与 Prompt Caching 的协同（2024–2026 必做）**：把**静态内容（角色、知识范围、规范）放前面、动态内容（检索文档、用户问题）放后面**，让前缀能命中 prompt caching——这是模板设计直接影响成本的关键（命中可省 \~90% input 成本，见 §11）。Anthropic 还需显式放 `cache_control` breakpoint。同时，需要结构化输出时用 **Structured Outputs / strict mode**（见 §2.1），不要在模板里"请返回 JSON"再正则解析。

### 9.2 环境隔离与渐进式部署

```
部署策略（从传统软件工程借鉴）：

Canary 发布（金丝雀部署）：
  10% 流量 → 新 prompt/model → 观察 24h 指标
  指标没恶化 → 50% 流量 → 再观察 → 100%

Feature Flag 集成：
  用 LaunchDarkly / 自建 flag 系统控制 prompt 版本
  flag: "qa_prompt_version" → values: "v2", "v3"
  好处：出问题 5 秒回滚，不需要重新部署

A/B 测试框架：
  用户随机分到实验组（新 prompt）和对照组（当前 prompt）
  对比核心指标：用户 👍率、任务完成率、回答被采纳率
  统计显著性检验：需要足够样本量
```

***

## 10. 降级容错与高可用架构

> **JD 原话：** "降级容错行为（fallback）、限流处理"

### 10.1 降级策略层次

```
Level 0: 无降级（最危险）
  主模型挂了 → 用户看到 500 错误
  
Level 1: 模型降级
  主模型挂了 → 自动切换到备选模型
  gpt-5.5 → gpt-5.4 → gpt-5.4-mini / claude-haiku-4-5

Level 2: 模型降级 + 缓存兜底
  所有模型都不可用 → 返回语义缓存中的相似回答
  标注 "此回答来自缓存，可能不是最新的"

Level 3: 模型降级 + 缓存 + 规则兜底
  以上都失败 → 返回预定义的静态回答
  "系统当前繁忙，请稍后再试。常见问题请查看帮助中心..."

Level 4: 优雅降级（完整方案）
  不只是回答，还要考虑：
  - 部分功能降级（复杂 Agent 切换到简单 QA）
  - 异步处理（用户无需等待，结果通过通知推送）
  - 用户体验（用 loading 动画代替白屏）
```

> ⚠️ **Agent 降级专属陷阱**：模型降级链必须保证候选模型都**支持原生 tool calling**——否则 failover 切到不支持 tool 的廉价模型，整个 agent 循环直接失效（见 §4.8）。降级时也要考虑 reasoning 能力差异（难任务降级到非推理模型可能完不成）。

### 10.2 断路器模式（Circuit Breaker）

```python
class LLMCircuitBreaker:
    """LLM 服务的断路器实现"""
    
    def __init__(self, failure_threshold=5, recovery_timeout=30, half_open_max=3):
        self.failure_threshold = failure_threshold
        self.recovery_timeout = recovery_timeout  # 秒
        self.half_open_max = half_open_max
        
        self.state = "CLOSED"  # CLOSED → OPEN → HALF_OPEN → CLOSED
        self.failure_count = 0
        self.last_failure_time = None
        self.half_open_successes = 0
    
    async def call(self, provider, request):
        if self.state == "OPEN":
            if self._should_attempt_recovery():
                self.state = "HALF_OPEN"
                self.half_open_successes = 0
            else:
                raise CircuitBreakerOpenError("Circuit breaker is OPEN")
        
        try:
            response = await provider(request)
            if self.state == "HALF_OPEN":
                self.half_open_successes += 1
                if self.half_open_successes >= self.half_open_max:
                    self.state = "CLOSED"
                    self.failure_count = 0
            return response
        except Exception as e:
            self.failure_count += 1
            self.last_failure_time = time.time()
            if self.failure_count >= self.failure_threshold:
                self.state = "OPEN"
                logger.critical(f"Circuit breaker OPENED for {provider}")
            raise
    
    def _should_attempt_recovery(self):
        if self.last_failure_time is None:
            return True
        return time.time() - self.last_failure_time > self.recovery_timeout
```

### 10.3 零停机 LLM 架构

```
核心组件：

1. Multi-Provider Load Balancing
   - 60% 流量给主 provider（如 OpenAI GPT-5.5）
   - 30% 给备选 provider（如 Claude Fable 5 / Opus 4.8）
   - 10% 给低成本 provider（如 DeepSeek，用于简单请求）
   - 动态权重：根据实时延迟和错误率调整

2. 智能健康检查
   - 每 30s 向每个 provider 发送测试 query
   - 连续 3 次失败 → 标记为 unhealthy → 自动摘除
   - 恢复后 → 渐进式重新引入流量（HALF_OPEN 模式）

3. 延迟感知路由
   - 持续跟踪每个 provider 的 P50/P95/P99 延迟
   - 延迟敏感请求 → 路由到当前最快的 provider

4. 自动重试策略
   - 429 (Rate Limit): 指数退避重试（1s → 2s → 4s → 8s）
   - 5xx (Server Error): 立即切换到备选 provider
   - 4xx (Client Error): 不重试（问题在你的请求本身）
   - Timeout: 重试 1 次 + 切换 provider
```

### 10.4 Rate Limiting 最佳实践

```python
# Token Bucket 限流器
class TokenBucketRateLimiter:
    """多维度限流器"""
    
    def __init__(self):
        self.buckets = {}  # key → (tokens, last_refill_time)
    
    def configure(self, key: str, rate: int, period: float):
        """rate: 每 period 秒允许的请求数"""
        self.buckets[key] = {
            "max_tokens": rate,
            "tokens": rate,
            "refill_rate": rate / period,  # tokens/second
            "last_refill": time.monotonic()
        }
    
    async def acquire(self, key: str) -> bool:
        bucket = self.buckets[key]
        now = time.monotonic()
        
        # 补充 token
        elapsed = now - bucket["last_refill"]
        bucket["tokens"] = min(
            bucket["max_tokens"],
            bucket["tokens"] + elapsed * bucket["refill_rate"]
        )
        bucket["last_refill"] = now
        
        if bucket["tokens"] >= 1:
            bucket["tokens"] -= 1
            return True
        
        return False

# 多维度的限流配置
rate_limiter = TokenBucketRateLimiter()

# 1. 全局限流：整体 QPS 不超过 100
rate_limiter.configure("global", rate=100, period=1)

# 2. 用户级限流：每个用户每分钟最多 20 个请求
rate_limiter.configure(f"user:{user_id}", rate=20, period=60)

# 3. Provider 级限流：遵循 API key 的 RPM/TPM 限制
rate_limiter.configure(f"provider:{provider}", rate=3500, period=60)
```

***

## 11. 限流与成本控制

### 11.1 成本控制全景策略

```
策略 1: 模型路由（Model Router）—— 核心降本手段
  ├── 简单问题路由到小模型（GPT-5.4-mini / Claude Haiku 4.5 / DeepSeek）
  ├── 复杂问题路由到大模型（GPT-5.5 / Claude Fable 5 / Claude Opus 4.8）
  └── 路由判断：用分类模型或规则引擎判断问题复杂度

策略 2: 语义缓存 —— 30-70% 成本节省
  ├── 相似问题直接返回缓存结果
  └── 详见第 12 章

策略 3: Prompt Caching（2024–2026 三家降本第一利器）
  ├── OpenAI: 自动缓存（≥1024 token 前缀，无需改代码），命中降 input 成本 ~90%、延迟 ~80%
  ├── Anthropic: 显式 cache_control breakpoint，cache read = base input 的 10%（即 -90%），TTL 5min/1h
  ├── Google Gemini: Implicit（自动不保证省钱）+ Explicit（手动 caches.create，保证省钱，cached input 远低于标准）
  └── 通用原则：静态前缀在前、动态在后；命中高的 RAG/Agent 场景可省 50–90% input 成本
  ⚠️ 必须在可观测性里记录 cached_tokens（见 §8），否则成本不可 trace

策略 4: Token 预算管理 + 隐藏 reasoning token 归因
  ├── 每次请求设定 max_tokens（推理模型要为 reasoning 预留，官方建议实验期 ≥25,000）
  ├── 监控 prompt 长度，发现趋势上升立即检查
  └── 用户级/会话级 token 配额
  ⚠️ 推理模型（GPT-5.x reasoning / Claude thinking / Gemini thinking）的 reasoning token **计费且部分细节不可见**
     —— 必须读 usage 里的 reasoning_tokens / thinking 输出统计做成本归因（OpenAI 中 reasoning_tokens 已含在 output_tokens，勿重复计）

策略 5: Batch 处理（三家都 50% 折扣）
  ├── OpenAI Batch: 50% off，目标 24h 内
  ├── Anthropic Message Batches: 50% off，大多 <1h，最长 24h（可与 prompt caching 叠加，命中率 30–98%）
  ├── Google Gemini Batch: 50% off
  └── 适用：离线评估、数据标注、大批量处理（§6 golden dataset 跑批首选）

策略 6: reasoning.effort / thinking 旋钮（2026 新增）
  ├── GPT-5.x: reasoning.effort = none/low/medium/high/xhigh
  ├── Claude 4.x: extended thinking + effort
  └── 简单任务调低 effort（省 reasoning token + 降 TTFT），难任务调高——这是 2026 调质量/成本/延迟的核心旋钮
```

### 11.2 模型路由（Model Router）实现思路

```python
class ModelRouter:
    """根据查询复杂度将请求路由到不同模型"""
    
    def __init__(self):
        self.complexity_classifier = None  # 可训练的复杂度分类器
        self.rules = [
            # (条件函数, 模型)
            (self._is_greeting, "gpt-5.4-mini"),
            (self._is_translation, "gpt-5.4-mini"),
            (self._is_simple_qa, "gpt-5.4-mini"),
            (self._needs_reasoning, "gpt-5.5"),
        ]
    
    def route(self, query: str, context: dict) -> str:
        for condition, model in self.rules:
            if condition(query, context):
                logger.info("Routing request", extra={"model": model, "query_len": len(query)})
                return model
        # 默认使用当前稳定的中等模型
        return "claude-sonnet-4-6"
    
    def _is_simple_qa(self, query, context):
        return len(query) < 50 and "?" in query
    
    def _needs_reasoning(self, query, context):
        reasoning_keywords = ["为什么", "分析", "比较", "判断", "推理"]
        return any(kw in query for kw in reasoning_keywords)
```

***

## 12. 语义缓存深度

> **面试核心问题：** "你做过 LLM 的缓存吗？语义缓存和普通缓存有什么区别？"

### 12.1 为什么需要语义缓存

```
场景对比：

传统精确缓存：
  用户问 "如何重置密码？" → 缓存 miss
  用户问 "怎么改密码？" → 缓存 miss（字符串不同）
  用户问 "密码忘记了怎么办？" → 缓存 miss

语义缓存：
  以上三个问题 → 都命中同一个缓存 → 直接返回结果
  节省 3 次 LLM 调用 = 省时间 + 省钱

成本节省：实验数据表明可以减少 30-68% 的 LLM API 调用
延迟提升：缓存命中时延迟从秒级降至毫秒级（10-100x 提升）
```

### 12.2 语义缓存架构

```python
import hashlib
import json
import logging
from dataclasses import dataclass
from typing import Optional

import numpy as np
from redis.commands.search.query import Query
from redis import Redis

logger = logging.getLogger(__name__)

@dataclass(frozen=True)
class CacheHit:
    response: str
    score: float

class SemanticCache:
    """基于 Redis Vector Search 的语义缓存。

    真实生产实现还需先用 FT.CREATE 建立 HASH + VECTOR HNSW 索引：
    ON HASH PREFIX 1 semcache:
      tenant_id TAG user_scope TAG query_hash TEXT response TEXT
      embedding VECTOR HNSW 6 TYPE FLOAT32 DIM <dim> DISTANCE_METRIC COSINE
    """
    
    def __init__(self, embedding_model, redis_client: Redis, 
                 similarity_threshold: float = 0.88):
        self.embed = embedding_model
        self.redis = redis_client
        self.threshold = similarity_threshold
    
    def _cache_key(self, tenant_id: str, user_scope: str, query: str) -> str:
        """使用稳定 hash，避免 Python 内置 hash 的跨进程随机化。"""
        query_hash = hashlib.sha256(query.encode()).hexdigest()
        raw = f"{tenant_id}:{user_scope}:{query_hash}".encode()
        return f"semcache:{hashlib.sha256(raw).hexdigest()}"

    def _embed(self, text: str) -> bytes:
        vector = np.asarray(self.embed.encode(text), dtype=np.float32)
        return vector.tobytes()
    
    async def get(self, tenant_id: str, user_scope: str, query: str) -> Optional[CacheHit]:
        """查询语义缓存"""
        query_vec = self._embed(query)
        
        # KNN 查询必须同时做租户/用户作用域过滤，防止跨客户缓存污染。
        q = (
            Query(
                "(@tenant_id:{$tenant_id} @user_scope:{$user_scope})"
                "=>[KNN 1 @embedding $vec AS vector_score]"
            )
            .sort_by("vector_score")
            .return_fields("response", "vector_score")
            .dialect(2)
        )
        results = self.redis.ft("semcache_idx").search(
            q,
            query_params={
                "tenant_id": tenant_id,
                "user_scope": user_scope,
                "vec": query_vec,
            },
        )
        
        if not results.docs:
            return None

        score = float(results.docs[0].vector_score)
        similarity = 1 - score  # cosine distance -> similarity
        if similarity < self.threshold:
            return None

        logger.info(
            "Semantic cache hit",
            extra={"tenant_id": tenant_id, "user_scope": user_scope, "similarity": similarity},
        )
        return CacheHit(response=results.docs[0].response, score=similarity)
    
    async def set(self, tenant_id: str, user_scope: str, query: str, response: str, ttl: int = 3600):
        """存入语义缓存"""
        key = self._cache_key(tenant_id, user_scope, query)
        query_hash = hashlib.sha256(query.encode()).hexdigest()
        self.redis.hset(
            key,
            mapping={
                "tenant_id": tenant_id,
                "user_scope": user_scope,
                "query_hash": query_hash,      # 不直接存原始问题，降低 PII 暴露面
                "response": response,          # 敏感回答应先脱敏或加密
                "embedding": self._embed(query),
                "metadata": json.dumps({"schema_version": 1}),
            },
        )
        self.redis.expire(key, ttl)
```

### 12.3 语义缓存优化十法（Redis LangCache 推荐）

| 技巧                        | 说明                            | 效果           |
| ------------------------- | ----------------------------- | ------------ |
| **1. 去除语义噪声**             | 去除 greeting、sign-off 等高频场景无关词 | 提高相似度计算精度    |
| **2. 领域适配 Embedding**     | 用微调过的 embedding 模型替代通用模型      | 提升召回率 5-15%  |
| **3. 长文本摘要**              | 先用小 LLM 摘要再缓存                 | 提高匹配率        |
| **4. 阈值调优**               | 从 0.88 开始调，平衡精度和召回            | 取决于业务容忍度     |
| **5. LLM Rerank**         | 检索后用轻量 LLM 验证匹配质量             | 减少误命中        |
| **6. Metadata 过滤**        | 按用户 ID、产品线、语言等过滤              | 防止跨用户/产品缓存污染 |
| **7. 自适应 TTL**            | 热点数据延长 TTL，冷数据短 TTL           | 提高命中率        |
| **8. 预缓存（Cache Warming）** | 预加载高频问题到缓存                    | 首次请求也能命中     |
| **9. 请求去重**               | 相同 session 内去重相同问题            | 减少并发重复调用     |
| **10. 监控命中率**             | 持续监控 hit rate，低于预期时排查         | 保证缓存有效性      |

***

## 13. 系统集成与延迟优化

### 13.1 AI 系统的后端集成模式

```
典型 AI 服务的后端架构：

                    ┌─────────────┐
                    │  API Gateway │ (认证、限流、路由)
                    └──────┬──────┘
                           │
              ┌────────────┼────────────┐
              ▼            ▼            ▼
        ┌──────────┐ ┌──────────┐ ┌──────────┐
        │  同步 API │ │ 异步队列  │ │  WebSocket│
        │ (FastAPI) │ │ (Kafka)  │ │  (SSE)   │
        └────┬─────┘ └────┬─────┘ └────┬─────┘
             │            │            │
        ┌────┴────┐  ┌────┴────┐  ┌────┴────┐
        │ LLM Svc │  │ Agent   │  │ Stream  │
        │         │  │ Worker  │  │ Handler │
        └────┬────┘  └────┬────┘  └────┬────┘
             │            │            │
        ┌────┴─────────────────────────┴────┐
        │          AI Service Layer          │
        │  Vector DB │ Redis Cache │ PG     │
        └───────────────────────────────────┘
```

**关键集成点：**

1. **异步任务处理：** 长时间 Agent 运行（> 30s）不适合同步 HTTP，用消息队列 + 回调/WebSocket 通知
2. **SSE (Server-Sent Events)：** 流式响应的标准协议，比 WebSocket 更轻量
3. **数据库设计：** 推荐结构
   - PostgreSQL：业务数据、用户、审计日志
   - Redis：会话状态、限流计数器、语义缓存
   - 向量数据库：文档嵌入和检索
4. **审计追踪：** 记录谁、什么时候、调用什么功能、用了多少 token、回答了什么

### 13.2 延迟优化的分层策略

```
优化金字塔（从最有效到最精细）：

Level 1: Streaming（最重要，用户感知最明显）
  TTFT (Time to First Token) 是用户感知延迟的核心指标
  即使总延迟不变，首 token 快 500ms → 用户满意度提升显著
  实现：使用 SSE，token 到达即发送

Level 2: 并行化
  独立操作并行执行：
  - 多个检索 query 并行执行
  - Embedding 和 classification 可以并行
  - 多个模型的调用之间没有依赖 → 并行

Level 3: 缓存
  - 语义缓存（相同/相似问题 10-100x 加速）
  - Prompt 缓存（Anthropic 原生支持，静态前缀缓存）
  - 结果预计算（高频 query 的答案提前算好）

Level 4: 模型选择
  - 简单任务用小模型（gpt-5.4-mini 比 gpt-5.5 快 3-5x）
  - 专用模型替代通用模型（分类用小模型，不用 GPT-5.5）

Level 5: 推理优化
  - vLLM/TGI 做自部署模型的推理加速
  - KV Cache 复用
  - Quantization (INT8/INT4 量化)
```

> ⚠️ **2026 新变量：推理模型的 TTFT 悖论**。reasoning/thinking 模型（GPT-5.x reasoning、Claude extended/adaptive thinking、Gemini thinking）**先思考再输出**，TTFT 显著高于非推理模型——这会抵消 Level 1 streaming 带来的首字节体验。应对：
>
> - 实时/对 TTFT 敏感的路径（语音、实时对话）用**非推理模型**或调低 `reasoning.effort=none/low`；
> - 重活（复杂推理、agent）走推理模型，但放异步/Batch 路径；
> - 用 preamble（如 OpenAI `phase:"commentary"`）让模型先吐开场白，掩盖思考延迟；
> - 记住 prompt caching 命中能把 latency 再降 \~80%（OpenAI 官方），是推理模型延迟的补救利器。

### 13.3 延迟 SLA 设计

```python
# 延迟目标分级
LATENCY_SLO = {
    "TTFT": {"p50": "500ms", "p95": "1.5s", "p99": "3s"},
    "Total Latency": {"p50": "3s", "p95": "10s", "p99": "20s"},
}

# 超时配置
TIMEOUTS = {
    "embedding_api": 2,      # 秒
    "vector_search": 1,       # 秒
    "reranker": 1,            # 秒
    "simple_llm_call": 15,    # 秒
    "complex_agent_step": 30, # 秒
    "total_request": 60,      # 秒，总上限
}
```

***

## 14. 加分项深度准备

### 14.1 Fine-tuning / Post-training 经验

如果在面试中被问到（作为加分项），你应该能讨论这些：

```
Fine-tuning 的适用场景（不是什么都 FT）：
  ✅ 需要特定格式输出但不想写复杂 prompt
  ✅ 领域术语和理解需要大幅提升
  ✅ 需要模仿特定风格/语气
  ❌ 只想让模型"知道"更多事实（应该用 RAG）
  ❌ 数据量不足（< 100 条高质量数据）

方法选择：
  - Full Fine-tuning：修改所有权重，需要大量 GPU
  - LoRA / QLoRA：只训练少量参数，消费级 GPU 可行
  - RLHF/DPO：对齐偏好，用于优化回答风格而非事实

数据准备：
  - 质量 > 数量
  - 100 条高质量数据 > 10000 条噪音数据
  - 覆盖多样性：不只训练"happy path"
  
评估微调效果：
  - 不能只看 loss — loss 低可能意味着过拟合
  - 必须在 holdout 评估集上对比 baseline
  - 关注泛化能力：训练集外的 case 表现如何
```

### 14.2 分布式系统/低延迟服务经验

```
可关联的分布式系统概念到 AI 工程：

一致性 → LLM 回答的一致性
  同一个问题应该给出一致的回答（temperature=0 + 缓存）

可用性 → LLM 服务的高可用
  多 provider、降级策略、断路器

分区容错 → AI 服务的容错
  部分依赖（如某类文档的向量库）挂了，服务还能部分工作

负载均衡 → 模型路由
  根据负载和延迟动态分配请求到不同模型

服务发现 → Provider 管理
  新增/摘除 LLM Provider 不需要改代码
```

### 14.3 行业经验（金融/医疗等）

如果目标公司的客户在这些行业：

**金融：**

- 合规要求：回答需要可审计、可解释
- 幻觉零容忍：金融建议出错的代价极高
- 数据隔离：客户数据不能用于训练他人模型
- SOC2 / PCI-DSS 合规

**医疗：**

- HIPAA 合规（美国）/ 个人信息保护法（中国）
- PHI 数据处理：不能泄露患者信息
- 临床决策支持：不能替代医生，必须标注"仅供参考"
- 可解释性比准确性更重要：医生需要知道"为什么"

**公共部门：**

- 透明度和可解释性要求极高
- 偏见检测：确保对所有人公平
- 审计追踪：每步决策可追溯

### 14.4 2026 新增加分项（展示你跟得上前沿）

**Context Engineering（上下文工程，2025 新概念，部分取代 "Prompt Engineering"）**

- 在长上下文 + agent 时代，核心技能从"写好一句 prompt"转向"**为模型组织正确的上下文**"：哪些文档、哪些历史、哪些工具结果、何时压缩/摘要、如何用 prompt caching 控成本。
- 能讲：你的系统如何动态组装上下文（RAG 检索 + 历史压缩 + 工具结果 + system prompt），而非把所有东西塞进一个静态 prompt。

**Test-Time Compute / Inference-Time Scaling**

- 概念：把更多推理算力花在"回答前先思考"换取质量，代价是更高延迟与隐藏的 reasoning token 成本。
- 工程含义：`reasoning.effort` / thinking 是调质量/成本/延迟的旋钮；离线重活可调高，实时路径调低或用非推理模型。

**Agentic 基础设施（MCP / Skills / A2A）**

- MCP 把工具集成从 N×M 降为 N+M，是系统集成的标准化方向（见 §4.3）。
- 多 provider failover 要适配三家**不同的** caching/batch/reasoning/tool 语义——这是真实的工程复杂度，能讲出踩坑就是加分。

**Agentic Coding / 长程任务**

- Claude Code、Cursor、Codex 类 agentic coding 工具背后的工程（agent loop + 长上下文 + 工具 + checkpoint），是 2025–2026 最热的落地形态之一。

***

## 15. 面试故事库（STAR 法则）

> **每个故事控制在 3-5 分钟，按 STAR 结构准备。**

### 故事 1：最复杂的 RAG 系统从 0 到 1

```
Situation（背景）:
  公司/项目需要构建一个 [具体领域] 的智能问答系统
  面临 [具体挑战：文档格式复杂 / 数据量大 / 延迟要求高]

Task（任务）:
  我需要设计并实现一个完整的 RAG pipeline
  目标：[具体指标，如准确率 > 85%，延迟 < 3s]

Action（行动）:
  1. 文档处理：选择了 [X] 库处理 PDF/HTML，因为 [Y]
  2. Chunking：尝试了 3 种策略，最终选择 [Z] 因为实验显示召回率最高
  3. Embedding：对比了 [A/B/C] 模型，在内部 benchmark 上 [X] 表现最好
  4. 检索：混合检索（向量 + BM25）+ Cohere Rerank
  5. 生成：设计了 prompt 模板，通过 A/B 测试迭代了 5 个版本
  6. 评估：构建了 200 条 golden dataset，使用 RAGAS 指标
  7. 上线后遇到 [具体问题]，通过 [具体方案] 解决

Result（结果）:
  - 准确率：82% → 91%
  - 延迟 P95：8s → 3s
  - 每月节省 [X] 美元 LLM 成本（通过缓存和模型路由）
  - 用户满意度从 [X] 提升到 [Y]
```

### 故事 2：构建评估体系

```
Situation:
  系统上线后，团队对 prompt 改动全靠"感觉"
  曾经一次 prompt 更新导致回答质量严重下降，但 3 天后才从用户投诉中发现

Task:
  需要建立一套自动化评估体系，确保每次变更都能在 5 分钟内看到质量变化

Action:
  1. Golden Dataset: 从 2000 条历史对话中筛选 + 领域专家标注，最终 300 条
  2. 指标设计: Faithfulness + Answer Relevancy + Context Precision
  3. LLM-as-Judge: 用 GPT-5.5 / Claude Fable 5 做自动评估，定期和人工评分校准
  4. CI/CD 集成: 每次 PR 自动运行评估，指标下降 > 3% 自动拦截
  5. 线上监控: 用户 👍👎 数据 + 自动抽样评估

Result:
  - 发现回退时间：3 天 → 5 分钟
  - 每月拦截了平均 4 次有问题的 prompt 变更
  - LLM-as-Judge 和人工评分相关系数 0.87
```

### 故事 3：生产环境事故处理

```
Situation:
  [某天某时] 系统突然出现大量用户投诉，回答质量异常

Task:
  快速定位问题、修复、建立防范机制

Action:
  1. 发现：从 LangFuse trace 发现检索回来的内容全是空（向量库挂了）
  2. 临时修复：切换到备选向量库（5 分钟）
  3. 根因分析：主向量库因 [具体原因] 挂了，但监控没有覆盖这个场景
  4. 长期修复：
     - 实现了向量库健康检查 + 自动 failover
     - 增加了检索结果为空的告警
     - 建立了降级机制：检索挂了时用缓存的 FAQ 回答
  5. 事后复盘：写了 post-mortem，分享了经验教训

Result:
  - 故障时间从 45 分钟降到后续同类故障的 30 秒（自动 failover）
  - 新增了 5 个监控指标
  - Post-mortem 被团队作为模板使用
```

### 故事 4：主动放弃的 AI 功能

```
Situation:
  PM 希望做一个"AI 自动生成代码并直接部署"的功能

Task:
  我需要评估技术可行性和风险，给出 honest 的建议

Action:
  1. 技术评估：LLM 生成的代码有 [X]% 包含安全漏洞或逻辑错误
  2. 成本评估：每次生成的代码需要 4-6 轮 agent 交互，成本高
  3. 用户研究：[Y]% 的开发者表示不信任 AI 直接部署的代码
  4. 替代方案：建议改为"AI 生成代码 + 人工审查 + 一键部署"
  5. 做了小规模 PoC 验证数据，用数据说服 PM

Result:
  - 避免了可能的生产事故
  - 替代方案上线后反而获得了更高的用户采用率
  - 在团队内建立了"用数据说话"的文化
```

***

## 16. 面试常见问题库

> 用法：不要死背答案。每个问题都按 **结论 → 取舍 → 生产事故/案例 → 如何验证** 的顺序准备 1-3 分钟版本；核心题再准备 8-10 分钟深讲版本。

### 16.1 RAG 相关

1. **"从 0 到 1 构建一个 RAG 系统，你会怎么做技术选型？"**
   → 按第 3 章的架构决策清单回答
2. **"Chunking 策略怎么选？chunk\_size 怎么定？"**
   → 讲 Recursive Splitter 的默认选择 + 如果精度不够怎么升级
3. **"向量检索和关键词检索各自适用什么场景？为什么需要混合检索？"**
   → 向量检索擅长语义匹配但弱于精确关键词；BM25 相反；RRF 融合
4. **"RAG 系统的答案怎么引用来源？"**
   → 在 prompt 中要求 LLM 标注引用编号 \[1] \[2]；用 structured output 强制返回引用
5. **"RAG 和 Long Context 模型（如 Gemini 2M、Claude 1M、GPT 1M）怎么选？"**
   → 2026 不要再答"上下文小所以要 RAG"（已过时）。给 nuanced 结论：
   - **长上下文"看起来"很强**：单 needle 在 1M 内 recall >99%，但**多 needle 显著掉点**（多 needle 基准 Gemini \~76% vs Claude \~24.5% @1M），说明"标称窗口 ≠ 有效窗口"。
   - **但生产角度 RAG 仍不可替代（四个硬约束）**：① **成本**：1M token 输入每次约 $2–$15/查询，RAG 典型 2K–10K 约 $0.00008–0.01，差 \~1250x；② **延迟/TTFT**：1M 首 token 20–30s vs RAG 200–500ms；③ **可靠性**：Lost in the Middle——信息在窗口中段时准确率显著下降（\~40%）；④ **可审计/可更新**：RAG chunk 粒度可溯源、可增量更新。
   - **2026 生产最佳实践：RAG 粗筛 → 长上下文精读**（混合）。先用 RAG 召回 top-k，再把这批（几十 K）喂长上下文模型做多跳推理。纯长上下文裸用只在"文档不大(<200K)、需全局推理、能命中 KV cache"的窄场景划算。
   - **决策表**：大语料 + 点查询 + 高 QPS → RAG；单篇大文档(<200K)多跳推理 → 长上下文裸用；复杂跨文档 + 大语料 → **混合（默认）**；要源可追溯/合规 → RAG。
6. **"RAG 召回率低，你会从哪里排查？"**
   → 先定位是 ingestion、chunking、embedding、query rewrite、retrieval、rerank 还是 prompt 使用问题。用 golden query 逐段打点：相关文档是否入库、chunk 是否完整、embedding 是否匹配语言/领域、metadata filter 是否误杀、top-k 是否太小、reranker 是否降错、答案生成是否忽略证据。
7. **"RAG 召回到了正确 chunk，但答案仍然错，原因可能是什么？"**
   → 可能是 context 拼接顺序差、引用冲突、prompt 没有强约束依据证据回答、模型过度依赖先验、chunk 缺少上下文、rerank 只看局部相关、用户问题需要多跳推理。解决：contextual chunk、small-to-big、证据压缩、结构化输出、拒答策略、faithfulness 评估。
8. **"Chunk overlap 越大越好吗？"**
   → 不是。overlap 提高边界信息保留，但会增加索引体积、重复召回、成本和噪声。应通过 golden set 比较 recall@k、MRR、answer faithfulness 和成本；常见做法是按段落/标题语义切分，再少量 overlap。
9. **"怎么处理表格、PDF、图片、代码这类非纯文本知识？"**
   → ingestion 阶段保留结构：表格转 Markdown/JSON 并保留行列语义；PDF 做版面解析和页码坐标；图片 OCR/视觉模型提取；代码按 symbol/文件/调用关系切分。检索结果要带 source、page、bbox、schema 等 metadata。
10. **"Embedding 模型怎么选？"**
    → 看语言覆盖、领域语义、维度/成本/延迟、最大输入长度、归一化方式、是否支持 batch、隐私合规。不要只看公开榜单，必须用自己的 query-document golden set 测 recall@k 和业务答案质量。
11. **"向量库怎么选？HNSW、IVF、DiskANN 分别适合什么？"**
    → HNSW 低延迟高召回但内存成本高；IVF 适合大规模、可调召回/速度；DiskANN/磁盘型适合超大规模降内存。面试重点是能讲清过滤、更新、扩容、备份、租户隔离和线上重建索引策略。
12. **"Metadata filtering 应该在向量检索前还是后？"**
    → 强权限/租户过滤必须前置或由向量库原生 filter 保证，不能后置补救；弱业务 filter 可前置缩小候选，也可后置 rerank。要警惕 filter 过窄导致召回为空。
13. **"Hybrid Search 的融合权重怎么调？"**
    → 默认用 RRF 比手调权重稳健；如果要调权重，用 query 分类或离线评估按领域优化。精确实体、编号、SKU、法律条款偏 BM25；语义问答、同义表达偏向量。
14. **"Reranker 值不值得上？"**
    → 高质量 RAG 通常值得。它提高 top-k 精度，尤其是混合检索候选多、query 复杂时；代价是延迟和成本。常见折中：召回 top-50/100，rerank 到 top-5/10，只对低置信度或高价值请求启用。
15. **"怎么做 Query Rewrite？会不会改坏用户意图？"**
    → rewrite 要可观测、可回退、可评估。保留原 query，生成 rewritten query、关键词、实体、过滤条件；用 hash 记录，不在普通日志存原文。对权限、时间、实体约束不能随意扩写。
16. **"HyDE 什么时候有效，什么时候危险？"**
    → 对短 query、概念型问题可提升召回；对事实敏感、强约束、合规场景可能引入虚构实体。应只用于召回辅助，不把 HyDE 文本当事实证据。
17. **"RAG 如何处理权限？"**
    → ingestion、索引、检索、缓存、引用都要带 tenant/user/resource ACL；权限过滤必须在检索阶段强制执行；语义缓存也要按 tenant/user scope 隔离，防止跨用户泄露。
18. **"如何设计 RAG 的引用和可追溯？"**
    → 每个 chunk 带 doc_id、version、page/section、offset/bbox、source_url、ingested_at；生成阶段强制引用证据 ID；返回答案同时返回引用列表和置信度，不允许引用未进入上下文的来源。
19. **"知识库更新后，如何保证检索结果一致？"**
    → 文档版本化、内容 hash、增量 re-embedding、双写/影子索引、索引别名切换、删除 tombstone、缓存失效。避免更新一半时线上混用新旧 chunk。
20. **"RAG 的缓存应该缓存什么？"**
    → 可缓存 query rewrite、embedding、retrieval candidates、rerank 结果、最终答案。最终答案缓存风险最高，必须带模型版本、prompt 版本、知识库版本、权限 scope、TTL 和失效策略。
21. **"RAG 如何处理多轮对话？"**
    → 不直接把全部历史拼进 query。先做 conversation state compression，提取当前轮独立问题、实体和约束，再检索。保留用户意图变更和指代消解的可追踪记录。
22. **"如何避免 RAG 幻觉？"**
    → 不是单靠 prompt。需要高召回、rerank、证据约束、引用校验、无法回答时拒答、faithfulness judge、人工抽检和线上反馈闭环。

### 16.2 Agent 相关

1. **"设计一个 Agent 系统，从架构上你会考虑哪些？"**
   → 按第 4 章和第 7 章回答：模型能力层 + 原生 tool calling loop + 工具契约 + 状态/记忆 + 安全边界 + 评估 + 可观测性 + 降级。
2. **"Agent 怎么防止无限循环？"**
   → 最大步数限制 + 连续相同 action 检测 + 总 token 上限
3. **"Tool calling 失败了怎么办？"**
   → 用原生 tool calling 时，失败不要抛异常打断——而是**把具体的、对模型有用的错误信息作为** **`tool_result`** **/** **`function_call_output`** **回传给模型**（如"未找到匹配文档，可尝试放宽 filters"），让模型据此自我纠错；配合重试上限。strict mode 能从源头消除"参数编造"类失败。
4. **"什么时候不该用 Agent？"**
   → 单步就能完成的任务 / 确定性逻辑（用规则） / 对延迟要求极高的场景 / 能用 Workflow（提示链/路由）预编排的——后者更可靠更省成本（§4.5）。
5. **"现在还会用 ReAct 的 Thought/Action/Observation 文本解析吗？"**
   → 不会作为生产主推。**原生 tool calling（`tools` + `tool_calls`）已取代 Action 解析，reasoning model 取代了显式 Thought**。ReAct 保留为"思路原型/历史演进"来答：它确立了"推理-行动交替"的循环范式，只是实现被原生 API 接管。
6. **"MCP 和 function calling 是什么关系？"**
   → MCP 是工具的**标准化交付/传输层**，底层仍是 function calling。MCP server 把声明的 tools 暴露给 client，client 转成 LLM 的 `tools` 数组——模型调用走的还是原生 tool calling。MCP 解决"工具怎么被发现、连接、复用"（把 N×M 集成降为 N+M），不取代调用机制。
7. **"Agent Loop 和 Workflow 的本质区别是什么？"**
   → Workflow 是预定义路径，可靠、可测、低成本；Agent Loop 是模型在约束内动态选择下一步，适合开放问题和工具组合。生产系统常用混合：外层 workflow 控制阶段，内层 harness 允许有限自主。
8. **"你为什么倾向自建 Harness，而不是直接用 LangGraph/CrewAI/AutoGen？"**
   → 核心是 KISS 和可控性。生产差异主要来自 prompts、tools、observability、model capability、harness constraints。框架可做 adapter 或局部便利层，但不能隐藏 token、tool args、trace、fallback 和权限边界。
9. **"Agent Harness 最小闭环需要哪些组件？"**
   → ProviderCapabilities、message/state store、tool registry、schema validation、executor loop、step limits、token budget、timeout、loop detection、tool result normalization、trace/replay、eval hooks、fallback。
10. **"工具 schema 怎么设计才适合模型调用？"**
    → 名称语义明确，description 写清适用/不适用场景，参数少而强约束，enum 优先，必填明确，错误信息可恢复；工具粒度不要太粗，也不要把数据库内部结构暴露给模型。
11. **"Tool permission 怎么做？"**
    → 工具分只读、低风险写、高风险写、外部副作用、敏感数据访问；高风险工具需要 policy check、人类确认、幂等键、审计日志和回滚方案。
12. **"Agent 如何处理需要用户确认的动作？"**
    → Loop 暂停并返回 pending_confirmation state，展示动作摘要、影响范围、关键参数和风险；用户确认后用同一 trace_id/resume_token 恢复，避免模型重新规划导致动作漂移。
13. **"Agent 的记忆怎么设计？"**
    → 区分短期工作记忆、会话摘要、长期用户偏好、外部事实知识。长期记忆必须可解释、可删除、可过期；不能把未经确认的模型输出直接写入长期事实记忆。
14. **"Agent 怎么做并发工具调用？"**
    → 对无依赖、只读、幂等工具可并发；对有顺序依赖或副作用工具串行。Harness 要记录 dependency、timeout、partial failure，并限制并发数避免打爆下游。
15. **"Agent 如何做状态持久化和恢复？"**
    → 每一步保存 messages、tool calls、tool results、预算、状态机节点和 trace。恢复时从最后一个已提交 step 继续，工具副作用用 idempotency key 防重复执行。
16. **"如何评估 Agent 是否变好？"**
    → 不只看最终答案，还看 trajectory：是否选对工具、参数是否正确、步数是否合理、是否触发不该触发的工具、是否能从错误中恢复、成本和延迟是否下降。
17. **"Multi-Agent 什么时候有价值？"**
    → 任务天然有角色分工、独立证据审查、规划/执行/验证分离时有价值。否则会增加沟通 token、延迟、不可控性。默认先单 Agent + verifier/evaluator，再考虑多 Agent。
18. **"Agent 怎么防 prompt injection？"**
    → 工具输出和用户内容都视为不可信数据；system/developer policy 不可被覆盖；检索内容放入隔离区；工具权限按服务端 policy 执行；对外部网页/文档中的指令做注入检测和降权。
19. **"Agent 怎么处理不确定性？"**
    → 允许澄清问题、拒答、请求更多权限或更多上下文。不要用自信语气掩盖低证据；trace 中记录 uncertainty reason，评估集中覆盖 ambiguous cases。
20. **"Agent 的成本为什么容易失控？怎么治理？"**
    → 循环步数、工具 fan-out、长上下文、重试和多 Agent 都会放大成本。治理靠预算、模型路由、缓存、最大步数、early stop、按任务价值启用深度推理。
21. **"模型升级会影响 Agent 吗？"**
    → 会。工具选择倾向、参数格式、拒答风格、推理长度都可能变。升级必须跑 trajectory eval、成本/延迟压测、shadow traffic 和回滚策略。
22. **"Agent 的日志能不能记录完整 prompt 和工具参数？"**
    → 默认不能。生产普通日志只记录 hash、长度、模型、版本、token、latency、tool name、错误码；原文和敏感 tool args 放受控审计存储，带权限、脱敏和保留期限。

### 16.3 评估相关

1. **"你怎么知道你的 AI 系统'变好了'？"**
   → 这是面试核心问题，按第 6 章三层评估体系回答，展示一个完整案例
2. **"LLM-as-Judge 有什么问题？你怎么解决的？"**
   → 讲 Position Bias、Verbosity Bias 等 + 缓解方案
3. **"评估集怎么建？多久更新一次？"**
   → Golden Dataset 构建五步法
4. **"离线评估和线上指标冲突时相信哪个？"**
   → 先判断评估集是否代表真实流量，线上是否受产品改动/流量结构/缓存影响。高分回答是建立映射：离线负责可回归，线上负责真实用户价值；冲突 case 要回灌评估集。
5. **"RAG 评估看哪些指标？"**
   → Retrieval：recall@k、MRR、nDCG、context precision；Generation：faithfulness、answer correctness、citation accuracy、refusal accuracy；系统：latency、cost、cache hit、用户反馈。
6. **"Agent 评估和普通问答评估有什么不同？"**
   → Agent 要评估过程轨迹，不只是最终输出。包括工具选择、参数、步数、恢复能力、权限遵守、副作用是否正确、是否过度调用工具。
7. **"如何构建 Golden Dataset？"**
   → 来源覆盖线上真实问题、专家构造边界、事故回放、竞品/历史失败 case；每条包含输入、期望证据、可接受答案、拒答条件、标签、难度、更新时间。
8. **"如何防止 eval overfitting？"**
   → train/dev/test 分层；隐藏集不参与日常 prompt 调参；定期加入新线上 case；关注分桶指标，不只看总分；用 adversarial cases 防止只优化表面表达。
9. **"LLM-as-Judge 的 judge prompt 怎么写？"**
   → 给 rubric、评分尺度、正反例、证据边界；强制先比较事实再给分；输出结构化 JSON；对顺序偏差做 A/B 顺序交换；关键指标用人工校准。
10. **"怎么评估引用是否正确？"**
    → 判断答案中的每个 claim 是否被引用 chunk 支持；引用是否来自上下文；是否引用过宽/错页；是否遗漏关键来源。可用规则 + judge + 抽样人工复核。
11. **"Human evaluation 怎么设计才不浪费？"**
    → 不让人泛泛打分。给明确 rubric、pairwise 比较、失败标签、置信度；优先抽样边界 case、低置信度 case、高价值流量和模型分歧 case。
12. **"评估如何进 CI/CD？"**
    → prompt/model/retrieval 变更触发离线 eval；设置按指标分桶的门禁；允许非关键指标波动阈值；保存报告、样例 diff 和回滚版本。
13. **"如何评估安全性？"**
    → prompt injection、越权检索、敏感信息泄露、危险工具调用、有害内容、拒答准确率。安全 eval 不能只靠普通质量集，要有红队 case。
14. **"A/B 测试 AI 功能有哪些坑？"**
    → 用户问题分布差、学习效应、缓存污染、长尾低频、人工反馈稀疏、指标延迟。需要分层随机、稳定实验单元、最小样本量和失败 case 审阅。
15. **"如何证明 prompt 改动有效？"**
    → prompt 版本 hash、变更说明、离线 eval diff、线上 shadow/A-B、失败样例对比、成本/延迟变化。不要只展示一两个 cherry-pick 示例。

### 16.4 生产工程相关

1. **"OpenAI API 挂了怎么办？"**
   → Multi-provider failover + 语义缓存 + 断路器模式
2. **"怎么降低 LLM 调用成本？"**
   → 模型路由 + 语义缓存 + Prompt Caching + Batch API
3. **"流式响应和非流式响应的 trade-off？"**
   → 流式：用户感知延迟低、可中断；非流式：实现简单、适合批量处理
4. **"你怎么设计 AI 系统的告警？"**
   → 错误率 > 5%、幻觉率突增 > 50%、TTFT P95 > 5s、成本突增 > 50%
5. **"LLM 调用超时怎么设置？"**
   → 区分连接超时、首 token 超时、整体超时、工具超时。按任务 SLA 设置预算，超时后返回缓存、降级模型、异步完成或友好失败。
6. **"多 Provider Failover 怎么做才不把问题扩大？"**
   → 先判断错误类型：4xx 不重试，429/5xx/timeout 可退避重试；备用模型能力、上下文、tool calling schema 可能不同，要有 ProviderCapabilities 和兼容测试。
7. **"流式响应中途失败怎么办？"**
   → UI 显示部分结果状态，服务端记录 partial generation；可重试时带 conversation state 恢复；已输出内容不可静默改写。对 tool calling 阶段可先不向用户流式暴露内部步骤。
8. **"如何设计幂等性？"**
   → 对写操作、外部副作用工具、异步任务使用 idempotency key；保存请求 hash、状态和结果；重试时返回已提交结果，避免重复扣费/发消息/下单。
9. **"AI 系统如何做灰度发布？"**
   → 按 prompt/model/retrieval/harness 版本灰度；shadow traffic 先跑不影响用户；A/B 分流；指标门禁；一键回滚 prompt 和模型路由。
10. **"Prompt 版本怎么管理？"**
    → 模板文件化、变量 schema、版本号/hash、变更说明、eval report 绑定、线上 trace 记录 prompt_template_version。敏感 prompt 权限控制。
11. **"如何处理模型输出 JSON 不合法？"**
    → 优先用 structured outputs / JSON schema / tool calling；仍失败时做有限 repair 和重试；记录 schema violation；不要用脆弱正则解析关键业务数据。
12. **"如何做 AI 服务的容量规划？"**
    → 估算 QPS、并发、平均/峰值 token、TTFT、下游限额、重试放大、缓存命中率；压测要包含长尾 prompt 和工具调用。
13. **"如何降低 P95/P99 延迟？"**
    → 模型路由、小模型预判、并行检索、embedding cache、rerank 条件化、prompt 压缩、流式、连接复用、区域就近、批处理和异步化。
14. **"如何做成本异常检测？"**
    → 按 tenant/user/model/feature/prompt_version 聚合 token 和费用；设置日预算、速率限制、异常 spike 告警；能追到具体 trace 和 prompt 版本。
15. **"日志和 Trace 应该记录什么？"**
    → trace_id、tenant_id、request_hash、model、provider、prompt version、tool name、token、latency、cost、cache、error code。避免记录原始 prompt、PII、密钥和敏感工具参数。
16. **"语义缓存命中错答案怎么办？"**
    → 缓存 key 加 tenant/user scope、prompt/model/kb version；设置相似度阈值、TTL、置信度；命中后仍可做轻量校验；错误 case 加入黑名单和 eval。
17. **"如何处理速率限制和配额？"**
    → 分用户、租户、模型、工具、成本维度限流；429 返回可重试时间；高价值请求优先级队列；内部重试必须遵守总体预算。
18. **"如何做数据隐私和合规？"**
    → 最小化发送给模型的数据；PII 检测/脱敏；供应商数据保留策略；审计日志；删除权；企业客户可选私有部署或指定区域。
19. **"AI 功能如何回滚？"**
    → 回滚不只是代码。要能回滚 prompt、模型、retrieval config、reranker、tool registry、index alias、cache version 和 feature flag。
20. **"生产事故复盘你会怎么写？"**
    → 时间线、影响范围、根因、检测缺口、缓解动作、长期修复、验证方式、owner 和 deadline。避免只写"加强测试"。

### 16.5 系统设计相关

1. **"设计一个支持 1000 QPS 的 AI 服务"**
   - API Gateway 做限流和认证
   - 多级缓存（语义缓存 + CDN）
   - 水平扩展：LLM 调用池 + 异步任务队列
   - Multi-provider 负载均衡
   - 降级策略和断路器
2. **"怎么处理长文档的 RAG？"**
   - 分层 chunking（文档 → 章节 → 段落）
   - Small-to-Big 检索策略
   - Late Chunking / ColBERT
3. **"设计一个企业知识库问答系统"**
   - ingestion pipeline：上传、解析、切分、embedding、索引、版本化
   - retrieval pipeline：权限过滤、hybrid search、rerank、context packing
   - generation：引用、拒答、结构化输出、多轮状态
   - eval/obs：golden set、trace、成本、反馈闭环
4. **"设计一个客服 Agent"**
   - 意图识别、知识库查询、订单/工单工具、人工转接
   - 高风险动作确认、权限控制、SLA、对话摘要
   - 失败降级：模板回复、转人工、异步处理
5. **"设计一个代码助手 Agent"**
   - repo indexing、symbol search、文件读取、测试执行、patch 生成
   - 沙箱权限、命令 allowlist、diff 审查、回滚
   - eval：编译通过率、测试通过率、patch acceptance、bug regression
6. **"设计一个文档处理流水线，支持 PDF/Word/网页"**
   - 异步任务队列、解析器插件、OCR/版面识别、结构化存储
   - 内容 hash 去重、增量更新、失败重试、人工修正入口
7. **"设计一个多租户 AI 平台"**
   - tenant isolation、API key 管理、预算/限流、模型路由
   - 数据隔离、索引隔离或 namespace、审计、用量账单
8. **"设计一个评估平台"**
   - dataset 管理、run 管理、judge 配置、报告 diff、CI 门禁
   - 支持人工标注、线上 case 回灌、指标分桶、历史趋势
9. **"设计一个模型路由器"**
   - 输入分类、SLA/成本/质量目标、provider capabilities
   - fallback、熔断、预算、实验分流、可观测性
10. **"设计一个支持长任务的 Agent 执行平台"**
    - 任务状态机、持久化 step、队列、checkpoint/resume
    - 人类确认、通知、取消、超时、幂等和审计

### 16.6 Prompt / 模型能力相关

1. **"Prompt 工程和传统工程有什么相同点和不同点？"**
   → 相同点是版本、测试、回滚、可观测；不同点是输出概率性强、模型升级会改变行为、评估更依赖样例和指标。
2. **"System prompt、developer prompt、user prompt 怎么分层？"**
   → system/developer 放稳定 policy、角色、工具边界和输出契约；user 放任务输入；检索内容和工具结果作为不可信上下文隔离处理。
3. **"Reasoning effort 应该如何设置？"**
   → 简单任务低 effort 控成本降延迟；复杂规划、多跳推理、高风险判断提高 effort。应由模型路由和任务分类自动决定，并在 trace 中记录。
4. **"什么时候用小模型？什么时候用大模型？"**
   → 小模型做分类、抽取、rewrite、低风险草稿；大模型做复杂推理、多工具规划、高价值客户请求。用 eval 和成本曲线决定，不靠感觉。
5. **"如何处理模型输出风格不稳定？"**
   → 结构化输出、模板约束、少量示例、后处理校验、版本固定。面向用户的文案可分离成 deterministic renderer。
6. **"Fine-tuning 解决什么，不解决什么？"**
   → 适合固定格式、风格、领域术语、分类抽取；不适合注入频繁变化的事实知识，也不能替代权限、检索和评估。
7. **"Prompt injection 和 jailbreak 的区别是什么？"**
   → prompt injection 多来自外部内容试图覆盖系统指令；jailbreak 多是用户直接诱导模型绕过安全策略。防护都需要边界隔离和服务端 policy。
8. **"如何设计结构化输出？"**
   → schema 小而稳定，字段语义明确，必要字段 required，枚举替代自由文本，输出后做 schema validation 和业务校验。

### 16.7 行为面试 / 项目经历相关

1. **"讲一个你把 AI 功能从 0 到 1 上线的经历。"**
   → 用 STAR：业务目标、约束、架构、评估、上线、事故/迭代、指标结果。
2. **"讲一个你主动否定或砍掉 AI 方案的经历。"**
   → 强调工程判断：质量不可证、成本不可控、延迟不达标、风险高于收益，并提出替代方案。
3. **"讲一次线上事故。"**
   → 不要甩锅模型。讲检测、止血、根因、长期修复、评估补洞和监控改进。
4. **"你和 PM/业务方如何协作迭代 prompt？"**
   → 业务目标转成评估 rubric；PM 可看样例和报告；工程负责版本、实验、回滚和安全边界。
5. **"你如何说服团队投入评估体系？"**
   → 讲没有评估导致的回归风险、模型升级风险、prompt 改动不可控；用小 golden set 快速证明价值。
6. **"你最自豪的工程 trade-off 是什么？"**
   → 选择一个质量/成本/延迟/可维护性的真实权衡，说明为什么不是追最复杂方案。
7. **"如果你入职后发现团队没有评估、没有 trace，你第一月做什么？"**
   → 先补最小闭环：trace 字段、golden set 50-100 条、prompt/model 版本、核心指标 dashboard、失败 case 复盘节奏。
8. **"你如何持续跟进模型和框架变化？"**
   → 官方文档、release note、基准复测、内部 ADR；不盲追新模型，先用项目 eval 验证。

***

## 17. 反问面试官的好问题

> 好问题能展示你的工程思维深度，也能帮你判断这个团队是否值得加入。
>
> 提问策略：先问 **现状**，再问 **最近一次真实案例**，最后问 **团队如何改进**。不要像审讯；选 5-8 个最贴合面试阶段的问题即可。

### 工程实践类

1. "团队目前怎么管理 prompt 版本？prompt 变更从开发到上线的流程是怎样的？"
2. "你们的评估体系是怎么设计的？评估集多大？多久跑一次？怎么处理评估和线上指标不一致的 case？"
3. "上一次 AI 系统的生产事故是什么原因？你们怎么修复的？事后做了什么来防止再次发生？"
4. "团队对模型升级的态度是怎样的？会持续追最新模型还是在某个版本上稳定？"
5. "一个 prompt / retrieval / tool schema 变更上线前，需要经过哪些检查？谁有最终放行权？"
6. "线上 trace 能追到 prompt version、model version、tool calls、token、cost 和引用来源吗？"
7. "如果用户反馈一个答案错了，团队从反馈到定位根因通常需要多久？"
8. "你们现在最常见的 AI 质量问题是召回差、幻觉、格式不稳定、工具调用错，还是延迟/成本？"
9. "有没有专门的失败案例库或 incident review，用来回灌评估集？"
10. "AI 功能的回滚粒度到什么程度？能单独回滚 prompt、模型、索引或工具吗？"
11. "你们如何处理线上敏感数据进入 prompt、日志和评估集的问题？"
12. "目前哪些 AI 任务是同步返回，哪些是异步任务？这个边界是怎么定的？"

### 技术决策类

1. "你们在什么情况下选择 RAG，什么情况下选择 Fine-tuning？能分享一个具体的案例吗？"
2. "团队内部对使用框架（如 LangChain）的态度是什么？有没有手写编排的偏好？"
3. "你们怎么平衡回答质量和延迟？有没有用户对延迟特别敏感的 feature？"
4. "Agent 这块你们更偏 workflow 编排，还是自建 Harness 式 loop？为什么？"
5. "目前模型供应商是单 provider 还是 multi-provider？切换 provider 时最大的问题是什么？"
6. "你们怎么定义模型路由策略？是按任务类型、客户等级、成本预算，还是质量置信度？"
7. "RAG 的 hybrid search、rerank、query rewrite 分别是默认开启还是按场景开启？"
8. "向量数据库选型时，团队最看重召回、延迟、成本、运维复杂度还是权限过滤能力？"
9. "长上下文模型出现后，你们对 RAG 架构做过哪些调整？"
10. "工具调用的权限模型是怎么设计的？哪些操作需要用户确认或人工审批？"
11. "你们是否用 MCP 或类似协议管理工具生态？如果用了，带来了什么实际收益和问题？"
12. "多 Agent 在你们业务中是真需求还是实验方向？有没有因为复杂度放弃过？"
13. "你们对 structured output / JSON schema 的依赖程度如何？有没有遇到 schema 漂移问题？"

### 评估与质量类

1. "团队最核心的离线质量指标是什么？它和业务指标之间的相关性验证过吗？"
2. "评估集里线上真实问题、专家构造问题、事故回放问题大概各占多少？"
3. "LLM-as-Judge 的 judge 本身怎么校准？有没有和人工标注做一致性评估？"
4. "模型升级、prompt 改动、检索参数改动分别会触发哪些 eval？"
5. "有没有隐藏测试集，防止团队为了公开评估集过拟合 prompt？"
6. "你们如何评估 Agent trajectory，而不仅仅是最终答案？"
7. "质量指标下降时，团队会自动阻断发布，还是只做提示？"
8. "有没有把用户反馈、客服工单、人工纠错自动回灌到评估体系？"
9. "安全评估覆盖 prompt injection、越权检索、敏感信息泄露和危险工具调用吗？"
10. "你们如何评估引用准确性？是人工抽检、规则校验，还是 judge？"

### 生产与可靠性类

1. "当前 AI 服务的 SLO 是什么？P95/P99 延迟、错误率、成本是否有明确目标？"
2. "如果主模型服务不可用，系统会怎么降级？用户能感知到什么？"
3. "语义缓存的命中率、误命中率和失效策略是怎么监控的？"
4. "成本预算是按团队、客户、功能还是用户维度管理？超预算后怎么处理？"
5. "限流策略会区分普通用户、企业客户、内部任务和批处理任务吗？"
6. "你们有没有经历过模型输出格式变化导致线上故障？后来怎么防？"
7. "RAG 索引更新是实时、准实时还是批处理？如何避免新旧索引混用？"
8. "AI 调用链路里最脆弱的下游依赖是什么？目前有什么熔断和隔离？"
9. "线上是否支持 shadow traffic 或 replay 来验证新模型/新 prompt？"
10. "你们的 observability dashboard 上，工程师每天最常看的三张图是什么？"

### 产品与业务类

1. "这个 AI 功能现在最重要的用户价值指标是什么？节省时间、提升转化、降低人工成本，还是提升留存？"
2. "用户最不能接受的是答案错、答案慢、无法解释，还是偶尔拒答？"
3. "AI 失败时产品体验如何设计？用户会看到降级、转人工还是明确的不确定性提示？"
4. "团队如何决定一个 AI 功能已经好到可以上线？"
5. "业务方参与评估和 prompt 迭代吗？他们使用什么工具反馈样例？"
6. "有没有某类客户或场景对合规、引用、可解释性要求特别高？"
7. "目前 AI 功能的主要增长瓶颈是技术质量、数据质量、产品分发，还是用户信任？"
8. "有没有因为模型能力不足或 ROI 不成立而主动下线/砍掉的 AI 功能？"

### 团队与文化类

1. "工程师和 PM 在 prompt 迭代中是怎么协作的？PM 能直接测试 prompt 吗？"
2. "团队怎么处理 'AI 做不到' 的情况？有没有功能因为技术不成熟被砍掉的经历？"
3. "新成员入职后，一般多久能开始独立负责一个 feature 从开发到上线？"
4. "团队鼓励工程师指出 AI 方案不可行吗？有没有这类决策被认真采纳的例子？"
5. "AI 工程师在这里更偏平台基础设施、产品功能交付，还是研究探索？比例大概怎样？"
6. "代码审查时，AI 相关变更会额外看哪些内容？prompt、eval、trace、成本都会看吗？"
7. "团队有没有固定的论文/产品/事故复盘分享机制？分享会如何转化为工程改进？"
8. "新人最容易踩的 AI 工程坑是什么？团队如何帮助他们补齐？"
9. "当模型供应商发布新能力时，团队是如何决定是否采用的？"
10. "这个岗位半年后做得好，最明显的成果会是什么？"

### 面试收尾类

1. "基于今天的交流，您觉得我在这个岗位上最大的匹配点和最大的风险点分别是什么？"
2. "如果我加入，前三个月最希望我解决的一个具体问题是什么？"
3. "这个团队当前最需要补强的是 RAG、Agent、评估、平台工程、还是产品化落地？"
4. "后续面试中，您建议我重点准备哪类案例或技术细节？"

***

## 18. 代码级自检清单

### 面试前必须能盲写的代码

- [ ] OpenAI/Anthropic SDK 的 Chat Completion（同步 + 流式）
- [ ] **Structured Outputs / strict mode**（用 Pydantic → JSON Schema 强约束输出）
- [ ] **原生 tool calling 的 Agent 循环**：消费 `tool_calls` → 执行 → 回传 `tool_result`/`function_call_output`（**不再是文本解析 ReAct**）
- [ ] 带指数退避重试的 LLM 调用封装
- [ ] Token 计数和预算管理（含 reasoning\_tokens 归因）
- [ ] 一个完整的 RAG pipeline（文档→切分→embedding→检索→生成，含 Contextual Retrieval）
- [ ] 语义缓存的 get/set 方法
- [ ] SSE 流式响应端点（FastAPI / GoFrame）
- [ ] 断路器模式的实现
- [ ] 评估函数（用 LLM-as-Judge 给一个回答打分，含 pairwise）

### 面试前必须能讲清楚原理的

- [ ] 原生 tool calling 机制（`tools`/`tool_choice`、function\_call vs tools 的演进、OpenAI vs Claude 差异）
- [ ] Structured Outputs / strict mode 的硬约束与"100% schema 遵守"
- [ ] MCP 的 Host/Client/Server 架构与四类 primitives，以及它与 tool calling 的关系
- [ ] HNSW vs IVF 的取舍（算法层面）+ 量化（scalar/binary/PQ）
- [ ] RRF（Reciprocal Rank Fusion）的公式和直觉（含 multi-query 融合）
- [ ] Embedding 的余弦相似度为什么有效 + Matryoshka 降维
- [ ] Temperature 和 Top-P 的区别和选择
- [ ] 为什么 LLM-as-Judge 有 bias（至少说出 5 种 + pairwise vs pointwise）+ 怎么缓解
- [ ] Transformer 的 Attention 机制（基础层面即可）
- [ ] Prompt Caching 三家机制差异（OpenAI 自动 / Anthropic 显式 breakpoint / Gemini explicit）
- [ ] reasoning token / test-time compute 的成本与延迟影响
- [ ] pass^k vs pass\@k（Agent 可靠性评估）
- [ ] OTel GenAI 语义约定（`gen_ai.*`）与弃用字段
- [ ] RAG 的 "Lost in the Middle" 问题 + 长上下文 vs RAG 的 nuanced 决策

***

## 附录 A：技术术语速查表

| 术语                                  | 一句话解释                                                                                       |
| ----------------------------------- | ------------------------------------------------------------------------------------------- |
| RAG                                 | 检索增强生成：先检索相关文档，再让 LLM 基于文档生成回答                                                              |
| Agent                               | 能自主调用工具的 LLM 系统                                                                             |
| Tool / Function Calling             | 模型原生输出结构化工具调用（`tool_calls`），取代旧的 `function_call`，取代文本解析式 ReAct 的 Action                     |
| Structured Outputs                  | 用 JSON Schema + strict mode 强制模型输出合法结构化结果（100% schema 遵守）                                   |
| MCP                                 | Model Context Protocol（Anthropic 2024.11 开源）：工具的标准化交付/传输协议，把 N×M 集成降为 N+M，底层仍是 tool calling |
| Agent Skills                        | Anthropic 2025 推出的高层"打包能力"（SKILL.md + 脚本/资源，渐进式披露），编排 MCP/Tools                             |
| ReAct                               | Reasoning + Acting：思考→行动→观察的循环模式（思路原型；现代由原生 tool calling + reasoning model 实现）              |
| Reasoning Model / Test-Time Compute | 先"思考"再输出的模型（GPT-5.x reasoning / Claude thinking），推理 token 计费且部分细节不可见                        |
| Context Engineering                 | 上下文工程（2025）：为模型组织正确上下文（检索/历史/工具结果/压缩），部分取代"prompt engineering"                              |
| Chunking                            | 文档切分：将长文档切成小块以便索引和检索                                                                        |
| Contextual Retrieval                | Anthropic 2024.09：给每个 chunk 生成上下文前缀再 embedding，检索失败率降 49%–67%                               |
| Late Chunking                       | Jina 2024：先对整文档 embedding 再按 chunk 边界池化，保留全局上下文                                             |
| Agentic RAG                         | 把 RAG 作为 agent 决策工具（按需检索、多轮、自反思），Self-RAG/CRAG 是其特例                                         |
| GraphRAG                            | Microsoft 2024：基于知识图谱 + 社区检测的 RAG，擅长跨文档/全局总结类问题                                             |
| Embedding                           | 文本转向量：将文本转换为固定长度的数字向量                                                                       |
| Matryoshka (MRL)                    | 训练让向量前缀也保持语义，从而可截断降维省存储/算力                                                                  |
| HNSW                                | 分层可导航小世界图：一种高效的向量近似搜索算法                                                                     |
| IVF                                 | 倒排文件索引：基于聚类的向量近似搜索方法                                                                        |
| 量化 Quantization                     | scalar/binary/PQ 压缩向量内存，配 oversample+rescore 保 recall                                       |
| RRF                                 | 倒数排名融合：将多个排序列表合并的算法（混合检索 + multi-query 融合）                                                  |
| TTFT                                | 首 Token 延迟：从发送请求到收到第一个 token 的时间（推理模型此项更高）                                                  |
| Prompt Caching                      | 缓存 prompt 静态前缀，命中降 input 成本 \~90%、延迟 \~80%（OpenAI 自动 / Anthropic 显式 / Gemini explicit）      |
| LoRA                                | 低秩适配：一种参数高效的微调方法                                                                            |
| Golden Dataset                      | 标准答案数据集：用于评估 AI 系统质量的标注数据                                                                   |
| pass^k                              | τ-bench 提出的可靠性指标：k 次独立运行全部成功的概率（比 pass\@k 严格）                                               |
| τ-bench                             | Sierra 的多轮工具调用 agent 基准（retail/airline/telecom…），含 dual-control、pass^k                      |
| RAGAS                               | RAG 评估框架：提供 faithfulness, relevancy 等指标 + Agent Goal Accuracy / Tool Call Accuracy          |
| OTel GenAI                          | OpenTelemetry GenAI 语义约定（`gen_ai.*`），LLM trace 字段的事实标准                                      |
| Circuit Breaker                     | 断路器：当依赖服务失败率达到阈值时自动停止调用的模式                                                                  |
| Semantic Cache                      | 语义缓存：基于语义相似度（不限于精确匹配）的缓存                                                                    |

***

## 附录 B：推荐阅读资源

### 必读论文（经典 + 2024–2026）

- "Retrieval-Augmented Generation for Knowledge-Intensive NLP Tasks" (Lewis et al., 2020)
- "ReAct: Synergizing Reasoning and Acting in Language Models" (Yao et al., 2022)
- "Judging LLM-as-a-Judge with MT-Bench and Chatbot Arena" (Zheng et al., 2023)
- "Justice or Prejudice? / CALM" (arXiv 2410.02736, 2024)——LLM-as-Judge 12 种 bias 系统量化
- "τ-bench" (arXiv 2406.12045, 2024)——多轮工具调用 agent 评估 + pass^k
- "From Local to Global: A Graph RAG Approach" (arXiv 2404.16130, 2024)——GraphRAG
- "Late Chunking in Long-Context Embedding Models" (arXiv 2409.04701, 2024)——Jina

### 必读工程文章（2024–2026 强烈推荐）

- Anthropic《Building Effective Agents》(2024.12)——workflow vs agent 六拓扑，权威
- Anthropic《Contextual Retrieval》(2024.09)：<https://www.anthropic.com/engineering/contextual-retrieval> ——上下文增强降 49%–67% 失败率，含 $1.02 / 百万 document tokens 成本假设
- Anthropic Prompt Engineering Guide / Context Engineering 指南
- OpenAI Models / Structured Outputs / Function Calling / Prompt Caching / Reasoning 指南：<https://platform.openai.com/docs/models>
- Claude Models Overview：<https://docs.anthropic.com/en/docs/about-claude/models/overview>
- Gemini API Models：<https://ai.google.dev/gemini-api/docs/models>
- MCP 官方规范：modelcontextprotocol.io

### 工具文档

- RAGAS: docs.ragas.io（注意 v0.4 架构变更）
- DeepEval: deepeval.com ｜ promptfoo: promptfoo.dev ｜ TruLens
- LangFuse: langfuse.com/docs ｜ Arize Phoenix
- OpenTelemetry GenAI Semantic Conventions: opentelemetry.io/docs/specs/semconv/gen-ai/
- Braintrust: braintrust.dev/docs

***

> **最后的话：**
> 这份文档的目标是让你在面试中展示的不是"我知道这些概念"，而是"我做过这些事、踩过这些坑、修过这些 bug"。
>
> 面试前一周，每天抽 2 小时，对着这份文档过一遍。不是为了背诵，而是为了把你自己的经历映射到这些问题上。
>
> **记住：讲你的故事，不是背我的答案。**
>
> Good luck. 🔧
